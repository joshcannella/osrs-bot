# DreamBot API — wrappers-entities

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### BoundaryObject

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.BoundaryObject → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.BoundaryObject → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.BoundaryObject → org.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.BoundaryObject → org.dreambot.api.wrappers.interactive.BoundaryObject  

**Methods:**
```java
int getFlags();
int getGridX();
int getGridY();
long getIndex();
int getOrientation();
int getPlane();
```

| Method | Description |
|--------|-------------|
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |

**Inherited methods:**
- From `GameObject`: `canReach`, `distance`, `distance`, `distance`, `exists`, `getActions`, `getAdjustToTerrain`, `getAlternativeIds`, `getAlternativeIDs`, `getAnimationId`, `getAnimationID`, `getAnimationObjectId`, `getBlockingMask`, `getBrightness`, `getComposite`
  ...and 58 more
- From `Entity`: `canReach`, `equals`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getReference`, `getSurroundingArea`, `hashCode`, `interact`, `interact`, `interact`, `interact`, `interact`, `interactForceLeft`
  ...and 2 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `getRegionId`

---

### Character

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.Character → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.Character → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.Character → org.dreambot.api.wrappers.interactive.Character  

**Methods:**
```java
boolean canAttack();
boolean exists();
int getAnimation();
int getAnimationDelay();
Character getCharacterInteractingWithMe();
List<Character> getCharactersInteractingWithMe();
int getCombatTime();
Direction getFacingDirection();
int getGridX();
int getGridY();
int getHeadFrame();
int getHealthPercent();
int[] getHitSplatCycles();
int[] getHitSplatIds();
List<Hitsplat> getHitsplats();
int[] getHitSplats();
int[] getHitSplatSpecials();
int[] getHitSplatTypes();
abstract int getIndex();
Character getInteractingCharacter();
int getInteractingIndex();
abstract int getLevel();
int getLocalX();
int getLocalY();
int getOrientation();
String getOverhead();
int getQueueSize();
int[] getQueueX();
int[] getQueueY();
int getRenderableHeight();
Tile getServerTile();
int getSmoothOrientation();
int getSpotAnimation();
int getStandAnimation();
int getTailFrame();
Tile getTile();
long getUID();
int getWalkAnimation();
int getWorldViewId();
int getX();
int getY();
int getZ();
boolean isAnimating();
boolean isHealthBarVisible();
boolean isInCombat();
boolean isInteractedWith();
boolean isInteracting(Character character);
boolean isMoving();
boolean isStandingStill();
```

| Method | Description |
|--------|-------------|
| `canAttack()` | Determines if this Character can be attacked. |
| `exists()` | Determines where or not this object exists in game. |
| `getAnimation()` | Gets the Character's current animation ID. |
| `getAnimationDelay()` | Deprecated. |
| `getCharacterInteractingWithMe()` | Gets the Character currently interacting with this Character. |
| `getCharactersInteractingWithMe()` | Gets a list of Character s interacting with this character instance. |
| `getCombatTime()` | Gets the current time in combat for this Character. |
| `getFacingDirection()` | Gets the closest facing direction for this Character in the game world. |
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |
| `getHeadFrame()` | Gets the head frame ID from the current animation. |
| `getHealthPercent()` | Get health percent of this Character if in combat. |
| `getHitSplatCycles()` | Get hit splat cycles for this Character. |
| `getHitSplats()` | Get hit splat for this Character. |
| `getHitSplatSpecials()` | Deprecated. |
| `getHitSplatTypes()` | Get hit splat types for this Character. |
| `getIndex()` | Gets the index of this Character. |
| `getInteractingCharacter()` | Gets interacting character for this Character. |
| `getInteractingIndex()` | Gets the interacting character's index. |
| `getLevel()` | Gets the Character's combat level. |
| `getLocalX()` | Gets local X coordinate of this Character. |
| `getLocalY()` | Gets local Y coordinate of this Character Character. |
| `getOrientation()` | Gets orientation for this Character. |
| `getOverhead()` | Gets overhead message for this Character. |
| `getQueueSize()` | Gets walking queue size for this Character. |
| `getQueueX()` | Get walking queue X coordinates. |
| `getQueueY()` | Get walking queue Y coordinates. |
| `getRenderableHeight()` | Gets the height of this character |
| `getServerTile()` | Gets the current tile according to the server |
| `getSmoothOrientation()` | Gets the smooth orientation for this Character. |
| `getStandAnimation()` | Gets the resting stand animation ID. |
| `getTailFrame()` | Gets the tail frame ID from the current animation. |
| `getTile()` | Gets tile of the object. |
| `getUID()` | Gets UID of this Character. |
| `getWalkAnimation()` | Gets walking animation ID. |
| `getX()` | Gets X coordinate of the given object. |
| `getY()` | Gets Y coordinate of the given object. |
| `getZ()` | Gets Z coordinate of the given object. |
| `isAnimating()` | Determines if this Character is animating. |
| `isHealthBarVisible()` | Determines if the health bar visible for this Character. |
| `isInCombat()` | Determines if this Character is in combat. |
| `isInteractedWith()` | Determines if there is a Character that is interacting with this Character |
| `isInteracting(Character character)` | Determines if this Character is interacting with the given argument. |
| `isMoving()` | Determines if this Character is moving. |
| `isStandingStill()` | Determines if the Character is standing still. |

**Inherited methods:**
- From `Entity`: `canReach`, `distance`, `distance`, `distance`, `equals`, `getActions`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getModel`, `getName`, `getReference`, `getSurroundingArea`, `getTileReference`
  ...and 11 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getId`, `getID`
- From `Locatable`: `canReach`, `getRegionId`, `tileDistance`

---

### Direction

**Package:** `org.dreambot.api.wrappers.interactive.util`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<Direction>org.dreambot.api.wrappers.interactive.util.Direction → java.lang.Enum<Direction> → org.dreambot.api.wrappers.interactive.util.Direction → org.dreambot.api.wrappers.interactive.util.Direction  

**Constants:**
```java
EAST
NORTH
NULL
SOUTH
WEST
```

**Methods:**
```java
int getId();
String toString();
static Direction valueOf(String name);
static Direction [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### Entity

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.Entity  

**Methods:**
```java
boolean canReach();
double distance();
double distance(Tile tile);
double distance(Entity entity);
boolean equals(Object obj);
boolean exists();
abstract String[] getActions();
Rectangle getBoundingBox();
Point getCenterPoint();
Point getClickablePoint();
String getLeftClickAction();
Model getModel();
abstract String getName();
Object getReference();
abstract int getRenderableHeight();
Area getSurroundingArea(int radius);
Tile getTile();
TileReference getTileReference();
abstract int getWorldViewId();
abstract boolean hasAction(String... actions);
int hashCode();
boolean interact();
boolean interact(String action);
boolean interact(String action, boolean rightClick, boolean forceRightClick);
boolean interact(String action, boolean rightClick, boolean forceRightClick, Condition interrupt);
boolean interact(Filter <String> filter);
boolean interactForceLeft(String action);
boolean interactForceRight(String action);
boolean isOnScreen();
double walkingDistance(Tile tile);
```

| Method | Description |
|--------|-------------|
| `canReach()` | Checks if this location is reachable by the local player without obstacles |
| `distance(Tile tile)` | Distance (as the crow flies) away from the given tile. |
| `distance(Entity entity)` | Distance away from the given entity. |
| `exists()` | Determines where or not this object exists in game. |
| `getBoundingBox()` | Gets the bounding box for the entity's model |
| `getCenterPoint()` | Gets center point of the object, which is used as a base location. |
| `getClickablePoint()` | Gets a clickable point within the object, used for interaction. |
| `getModel()` | Gets the model of the entity |
| `getName()` | Gets the name of the entity |
| `getReference()` | Gets the object used as the reference. |
| `getTile()` | Gets tile of the object. |
| `getTileReference()` | Gets tile reference of the given object. |
| `hasAction(String... actions)` | Checks whether given actions are in the list of menu actions for an entity |
| `interact()` | Interacts with the given object using a default left click. |
| `interact(String action)` | Interacts with the given object using an action. |
| `interact(Filter <String> filter)` | Interacts with the given object using the first matching action. |
| `interactForceLeft(String action)` | Interacts with current object by forcing a left click. |
| `interactForceRight(String action)` | Interacts with current object by forcing a right click and interacting with menu (Interact using Menus) |
| `isOnScreen()` | Checks whether the entity is visible on the game screen |
| `walkingDistance(Tile tile)` | Walking distance based on pathfinding to the given tile. |

**Inherited methods:**
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getId`, `getID`
- From `Locatable`: `canReach`, `getGridX`, `getGridY`, `getRegionId`, `getX`, `getY`, `getZ`, `tileDistance`

---

### FloorDecoration

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.FloorDecoration → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.FloorDecoration → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.FloorDecoration → org.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.FloorDecoration → org.dreambot.api.wrappers.interactive.FloorDecoration  

**Methods:**
```java
int getFlags();
int getGridX();
int getGridY();
long getIndex();
int getOrientation();
int getPlane();
```

| Method | Description |
|--------|-------------|
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |

**Inherited methods:**
- From `GameObject`: `canReach`, `distance`, `distance`, `distance`, `exists`, `getActions`, `getAdjustToTerrain`, `getAlternativeIds`, `getAlternativeIDs`, `getAnimationId`, `getAnimationID`, `getAnimationObjectId`, `getBlockingMask`, `getBrightness`, `getComposite`
  ...and 58 more
- From `Entity`: `canReach`, `equals`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getReference`, `getSurroundingArea`, `hashCode`, `interact`, `interact`, `interact`, `interact`, `interact`, `interactForceLeft`
  ...and 2 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `getRegionId`

---

### GameObject

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.GameObject  

**Methods:**
```java
boolean canReach(Tile tile);
double distance();
double distance(Tile tile);
double distance(Entity entity);
boolean exists();
String[] getActions();
int getAdjustToTerrain();
int[] getAlternativeIds();
int[] getAlternativeIDs();
int getAnimationId();
int getAnimationID();
int getAnimationObjectId();
int getBlockingMask();
int getBrightness();
org.dreambot.internal.cache.wrappers.def.ObjectDefinition getComposite();
int getContrast();
int getCurrentOrientation();
abstract int getFlags();
int getHeight();
int getId();
abstract long getIndex();
List<Tile> getInteractableFrom();
int getInteractType();
int getLocalX();
int getLocalY();
int getMapAreaId();
int getMapSceneId();
int getMapSceneID();
int getMiniMapIcon();
Model getModel();
short[] getModelColors();
int getModelSizeX();
int getModelSizeY();
int getModelSizeZ();
short[] getModifiedModelColors();
String getName();
int[] getObjectModelTypes();
List<Tile> getObjectTiles();
int getOffsetX();
int getOffsetY();
int getOffsetZ();
abstract int getOrientation();
Map<Integer,Object> getParams();
abstract int getPlane();
int getRealId();
int getRealID();
int getRenderableHeight();
short[] getReTextureToFind();
short[] getReTextureToReplace();
int getSupportsItems();
List<Tile> getSurrounding();
Tile getTile();
TileReference getTileReference();
int getVarbitId();
int getVarbitID();
int getVarpId();
int getVarpID();
int getWallOrDoor();
int getWallThickness();
int getWidth();
int getWorldViewId();
int getX();
int getY();
int getZ();
boolean hasAction(String... actions);
boolean hasChildDefinitions();
boolean isBlocksProjectiles();
boolean isCastsShadows();
boolean isImpenetrableSolid();
boolean isNonFlatShading();
boolean isObstructsGround();
boolean isOccludes();
boolean isRandomizeAnimationStart();
void setTile(Tile t);
int tileDistance(Tile tile);
String toString();
double walkingDistance(Tile tile);
```

| Method | Description |
|--------|-------------|
| `canReach(Tile tile)` | Checks if this location is reachable by the provided tile without obstacles |
| `distance(Tile tile)` | Distance (as the crow flies) away from the given tile. |
| `distance(Entity entity)` | Distance away from the given entity. |
| `exists()` | Determines where or not this object exists in game. |
| `getAlternativeIDs()` | Deprecated. |
| `getAnimationID()` | Deprecated. |
| `getBlockingMask()` | Deprecated. |
| `getCurrentOrientation()` | Returns the current orientation of an object based on its set flags and original orientation. |
| `getHeight()` | Gets the Tile 2D height of the object (furnace is 2x3, 3 is height) |
| `getId()` | Gets ID number of the given object. |
| `getInteractableFrom()` | Gets a list of valid tiles your player could be standing on to interact with the game object (not including diagonals) |
| `getMapSceneID()` | Deprecated. |
| `getModel()` | Gets the model of the entity |
| `getName()` | Gets the name of the entity |
| `getRealID()` | Deprecated. |
| `getRenderableHeight()` | Gets the model's height |
| `getSurrounding()` | Gets the surrounding tiles (not including diagonals) around the GameObject |
| `getTile()` | Gets tile of the object. |
| `getTileReference()` | Gets tile reference of the given object. |
| `getVarbitID()` | Deprecated. |
| `getVarpID()` | Deprecated. |
| `getWidth()` | Gets the Tile 2D width of the object (furnace is 2x3, 2 is width) |
| `getX()` | Gets X coordinate of the given object. |
| `getY()` | Gets Y coordinate of the given object. |
| `getZ()` | Gets Z coordinate of the given object. |
| `hasAction(String... actions)` | Checks whether given actions are in the list of menu actions for an entity |
| `tileDistance(Tile tile)` | Distance (Chebyshev/Tchebychev) away from the given tile. |
| `walkingDistance(Tile tile)` | Walking distance based on pathfinding to the given tile. |

**Inherited methods:**
- From `Entity`: `canReach`, `equals`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getReference`, `getSurroundingArea`, `hashCode`, `interact`, `interact`, `interact`, `interact`, `interact`, `interactForceLeft`
  ...and 2 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `getGridX`, `getGridY`, `getRegionId`

---

### Identifiable

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** interface  

**Methods:**
```java
int getId();
default int getID();
String getName();
```

| Method | Description |
|--------|-------------|
| `getId()` | Gets ID number of the given object. |
| `getID()` | Deprecated. |
| `getName()` | Gets the name of the given object. |

---

### Interactable

**Package:** `org.dreambot.api.wrappers.interactive.interact`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.interact.Interactable  

**Methods:**
```java
abstract Point getCenterPoint();
abstract Point getClickablePoint();
boolean hover(String action, Condition interrupt);
boolean interact();
boolean interact(String action);
boolean interact(String action, boolean rightClick, boolean forceRightClick);
boolean interact(String action, boolean rightClick, boolean forceRightClick, Condition interrupt);
boolean interact(Filter <String> filter);
boolean interactForceLeft(String action);
boolean interactForceRight(String action);
boolean rightClick(Condition interrupt);
```

| Method | Description |
|--------|-------------|
| `getCenterPoint()` | Gets center point of the object, which is used as a base location. |
| `getClickablePoint()` | Gets a clickable point within the object, used for interaction. |
| `interact()` | Interacts with the given object using a default left click. |
| `interact(String action)` | Interacts with the given object using an action. |
| `interact(Filter <String> filter)` | Interacts with the given object using the first matching action. |
| `interactForceLeft(String action)` | Interacts with current object by forcing a left click. |
| `interactForceRight(String action)` | Interacts with current object by forcing a right click and interacting with menu (Interact using Menus) |

---

### Locatable

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** interface  

**Methods:**
```java
default boolean canReach();
default boolean canReach(Tile tile);
default double distance(Tile tile);
default int getGridX();
default int getGridY();
default int getRegionId();
Tile getTile();
default TileReference getTileReference();
default int getX();
default int getY();
default int getZ();
default int tileDistance(Tile tile);
default double walkingDistance(Tile tile);
```

| Method | Description |
|--------|-------------|
| `canReach()` | Checks if this location is reachable by the local player without obstacles |
| `canReach(Tile tile)` | Checks if this location is reachable by the provided tile without obstacles |
| `distance(Tile tile)` | Distance (as the crow flies) away from the given tile. |
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |
| `getRegionId()` | Gets the region id of the given object. |
| `getTile()` | Gets tile of the object. |
| `getTileReference()` | Gets tile reference of the given object. |
| `getX()` | Gets X coordinate of the given object. |
| `getY()` | Gets Y coordinate of the given object. |
| `getZ()` | Gets Z coordinate of the given object. |
| `tileDistance(Tile tile)` | Distance (Chebyshev/Tchebychev) away from the given tile. |
| `walkingDistance(Tile tile)` | Walking distance based on pathfinding to the given tile. |

---

### Model

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.Model → org.dreambot.api.wrappers.interactive.Model  

**Methods:**
```java
Point calculateCenterPoint();
java.awt.geom.Area calculateModelArea();
void drawWireFrame(java.awt.Graphics graphics);
boolean exists();
int getAnimationHeightOffset();
Entity getEntity();
int getGridX();
int getGridY();
java.awt.Shape getHullBounds();
java.awt.Shape getHullBounds(float scale);
int getIndicesCount();
int[] getIndicesX();
int[] getIndicesY();
int[] getIndicesZ();
int[] getOriginalX();
int[] getOriginalY();
int[] getOriginalZ();
byte getOverrideAmount();
byte getOverrideHue();
byte getOverrideLuminance();
byte getOverrideSaturation();
int getTileHeight();
int getVertexCount();
int[][] getVertexMap();
int[][] getVertexMap(int gridX, int gridY);
int[][] getVertexMap(int gridX, int gridY, boolean predictMovement);
List<Point> getVertexPoints();
List<Point> getVertexPoints(int gridX, int gridY);
int[] getVerticesX();
int[] getVerticesY();
int[] getVerticesZ();
void setAnimationHeightOffset(int animationHeightOffset);
void setEntity(Entity entity);
void setGridLocation(int gridX, int gridY);
void setGridX(int gridX);
void setGridY(int gridY);
void setIndicesCount(int indicesCount);
void setIndicesX(int[] indicesX);
void setIndicesY(int[] indicesY);
void setIndicesZ(int[] indicesZ);
void setOriginalX(int[] originalX);
void setOriginalY(int[] originalY);
void setOriginalZ(int[] originalZ);
void setOverrideAmount(byte overrideAmount);
void setOverrideHue(byte overrideHue);
void setOverrideLuminance(byte overrideLuminance);
void setOverrideSaturation(byte overrideSaturation);
void setVertexCount(int verticesCount);
void setVerticesX(int[] verticesX);
void setVerticesY(int[] verticesY);
void setVerticesZ(int[] verticesZ);
```

| Method | Description |
|--------|-------------|
| `calculateCenterPoint()` | Calculates the true center point of the model using its vertices. |
| `calculateModelArea()` | Calculates the model area using its vertices. |
| `drawWireFrame(java.awt.Graphics graphics)` | Draws wire frame of this object. |
| `exists()` | Used to determine if this model exists and is valid. |
| `getGridX()` | Gets grid X of this model. |
| `getGridY()` | /** Gets grid Y of this model. |
| `getHullBounds()` | Gets hull bounds of this object with a default scale of 1.0F. |
| `getHullBounds(float scale)` | Gets hull bounds of this object with a specified scale. |
| `getIndicesCount()` | Get indices count. |
| `getIndicesX()` | Get indices x. |
| `getIndicesY()` | Get indices y. |
| `getIndicesZ()` | Get indices z. |
| `getOriginalX()` | Get original x. |
| `getOriginalY()` | Get original y. |
| `getOriginalZ()` | Get original z. |
| `getTileHeight()` | Calculates the tile height for offset specified tile. |
| `getVertexCount()` | Get vertex count. |
| `getVertexMap()` | Get vertex locations stored in a 2D array (flat map) at the current grid location. |
| `getVertexMap(int gridX, int gridY)` | Get vertex locations stored in a 2D array (flat map) at the specified grid location. |
| `getVertexMap(int gridX, int gridY, boolean predictMovement)` | Get vertex locations stored in a 2D array (flat map) at the specified grid location. |
| `getVertexPoints()` | Gets model vertex points at the current set grid location. |
| `getVertexPoints(int gridX, int gridY)` | Gets model vertex points at the specified grid location. |
| `getVerticesX()` | Get vertices x. |
| `getVerticesY()` | Get vertices y. |
| `getVerticesZ()` | Get vertices z. |
| `setGridLocation(int gridX, int gridY)` | Sets grid location (x/y) of this model. |
| `setGridX(int gridX)` | Sets grid X. |
| `setGridY(int gridY)` | Sets grid Y. |
| `setIndicesCount(int indicesCount)` | Sets indices count. |
| `setIndicesX(int[] indicesX)` | Sets indices x. |
| `setIndicesY(int[] indicesY)` | Sets indices y. |
| `setIndicesZ(int[] indicesZ)` | Sets indices z. |
| `setOriginalX(int[] originalX)` | Sets original x. |
| `setOriginalY(int[] originalY)` | Sets original y. |
| `setOriginalZ(int[] originalZ)` | Sets original z. |
| `setVertexCount(int verticesCount)` | Sets vertex count. |
| `setVerticesX(int[] verticesX)` | Sets vertices x. |
| `setVerticesY(int[] verticesY)` | Sets vertices y. |
| `setVerticesZ(int[] verticesZ)` | Sets vertices z. |

---

### NPC

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.Characterorg.dreambot.api.wrappers.interactive.NPC → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.Characterorg.dreambot.api.wrappers.interactive.NPC → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.Characterorg.dreambot.api.wrappers.interactive.NPC → org.dreambot.api.wrappers.interactive.Character → org.dreambot.api.wrappers.interactive.NPC → org.dreambot.api.wrappers.interactive.NPC  

**Methods:**
```java
boolean exists();
String[] getActions();
int[] getAdditionalModels();
Area getArea();
int getCategory();
NpcOverride getChatHeadModelOverride();
int[] getChildIds();
int[] getChildIDs();
org.dreambot.internal.cache.wrappers.def.NPCDefinition getComposite();
int[] getCurrentOverheadIcons();
short[] getCurrentOverheadSpriteIndices();
int getDegreesToTurn();
int[] getHeadIconArchiveIds();
short[] getHeadIconSpriteIndex();
int getHeightScale();
int getId();
int getIdleAnimation();
int getIndex();
int getLevel();
int getLightModifier();
int[] getModelIds();
int[] getModelIDs();
NpcOverride getModelOverride();
String getName();
Map<Integer,Object> getParams();
int getRealId();
int getRealID();
short[] getRecoloredOriginal();
short[] getRecoloredTarget();
short[] getRecolorOriginal();
short[] getRecolorTarget();
short[] getRetextureToFind();
short[] getRetextureToReplace();
int getShadowModifier();
int getSize();
Tile getTrueTile();
int getTurn180Animation();
int getTurn90CCWAnimation();
int getTurn90CWAnimation();
int getVarbitId();
int getVarbitID();
int getVarpId();
int getVarpID();
int getWalkingAnimation();
int getWidthScale();
boolean hasAction(String... actions);
boolean hasRenderPriority();
boolean isClickable();
boolean isDrawMinimapDot();
boolean isFollower();
boolean isLowPriorityFollowerOps();
boolean isRotationFlag();
void setIndex(int index);
String toString();
```

| Method | Description |
|--------|-------------|
| `exists()` | Determines where or not this object exists in game. |
| `getCategory()` | Deprecated. |
| `getChatHeadModelOverride()` | Returns the NPC's chathead model override, contains model overriding data |
| `getChildIDs()` | Deprecated. |
| `getCurrentOverheadIcons()` | Returns the overhead icon archive IDs for the currently available overhead icon |
| `getCurrentOverheadSpriteIndices()` | Returns the overhead icon sprite indices for the currently available overhead icon |
| `getId()` | Gets ID number of the given object. |
| `getIndex()` | Gets the index of this Character. |
| `getLevel()` | Gets the Character's combat level. |
| `getModelIDs()` | Deprecated. |
| `getModelOverride()` | Returns the NPC's model override, contains model overriding data |
| `getName()` | Gets the name of the entity |
| `getRealID()` | Deprecated. |
| `getTrueTile()` | Gets the tile that loot will drop on, adjusts NPC tile based on NPC size. |
| `getVarbitID()` | Deprecated. |
| `getVarpID()` | Deprecated. |
| `hasAction(String... actions)` | Checks whether given actions are in the list of menu actions for an entity |
| `setIndex(int index)` | Deprecated. |

**Inherited methods:**
- From `Character`: `canAttack`, `getAnimation`, `getAnimationDelay`, `getCharacterInteractingWithMe`, `getCharactersInteractingWithMe`, `getCombatTime`, `getFacingDirection`, `getGridX`, `getGridY`, `getHeadFrame`, `getHealthPercent`, `getHitSplatCycles`, `getHitSplatIds`, `getHitsplats`, `getHitSplats`
  ...and 31 more
- From `Entity`: `canReach`, `distance`, `distance`, `distance`, `equals`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getModel`, `getReference`, `getSurroundingArea`, `getTileReference`, `hashCode`, `interact`
  ...and 8 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `canReach`, `getRegionId`, `tileDistance`

---

### NameComposite

**Package:** `org.dreambot.api.wrappers.interactive.composite`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.composite.NameComposite → org.dreambot.api.wrappers.interactive.composite.NameComposite  

**Methods:**
```java
String getFormatted();
String getName();
```

| Method | Description |
|--------|-------------|
| `getFormatted()` | Deprecated. |
| `getName()` | Deprecated. |

---

### Player

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.Characterorg.dreambot.api.wrappers.interactive.Player → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.Characterorg.dreambot.api.wrappers.interactive.Player → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.Characterorg.dreambot.api.wrappers.interactive.Player → org.dreambot.api.wrappers.interactive.Character → org.dreambot.api.wrappers.interactive.Player → org.dreambot.api.wrappers.interactive.Player  

**Methods:**
```java
boolean exists();
String[] getActions();
int[] getAppearance();
int[] getBodyColors();
Rectangle getBoundingBox();
PlayerComposite getComposite();
List<Item> getEquipment();
int getId();
int getIndex();
int getLevel();
String getName();
NameComposite getNameComposite();
int getOverheadIcon();
int getSkullIcon();
int getTeam();
boolean hasAction(String... actions);
String hashed();
String hashed(String prepend, String append);
boolean isHidden();
boolean isSkulled();
void setIndex(int index);
String toString();
```

| Method | Description |
|--------|-------------|
| `exists()` | Determines where or not this object exists in game. |
| `getActions()` | Gets the actions available for a player. |
| `getBoundingBox()` | Gets the bounding box for the entity's model |
| `getComposite()` | Deprecated. |
| `getEquipment()` | Gets a list of Items that the player is wearing and adjusting the value to translate from appearance to item. |
| `getId()` | Gets ID number of the given object. |
| `getIndex()` | Gets the index of this Character. |
| `getLevel()` | Gets the Character's combat level. |
| `getName()` | Gets the name of the entity |
| `getNameComposite()` | Deprecated. |
| `hasAction(String... actions)` | Determines if player contains at least one of the following actions. |
| `isHidden()` | Deprecated. |

**Inherited methods:**
- From `Character`: `canAttack`, `getAnimation`, `getAnimationDelay`, `getCharacterInteractingWithMe`, `getCharactersInteractingWithMe`, `getCombatTime`, `getFacingDirection`, `getGridX`, `getGridY`, `getHeadFrame`, `getHealthPercent`, `getHitSplatCycles`, `getHitSplatIds`, `getHitsplats`, `getHitSplats`
  ...and 31 more
- From `Entity`: `canReach`, `distance`, `distance`, `distance`, `equals`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getModel`, `getReference`, `getSurroundingArea`, `getTileReference`, `hashCode`, `interact`, `interact`
  ...and 7 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `canReach`, `getRegionId`, `tileDistance`

---

### PlayerComposite

**Package:** `org.dreambot.api.wrappers.interactive.composite`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.composite.PlayerComposite → org.dreambot.api.wrappers.interactive.composite.PlayerComposite  

---

### RSAction

**Package:** `org.dreambot.api.wrappers.interactive.action`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.action.RSAction → org.dreambot.api.wrappers.interactive.action.RSAction  

**Methods:**
```java
MenuRow getMenuRow();
int getMouseX();
int getMouseY();
void setMenuRow(MenuRow menuRow);
void setMouseX(int mouseX);
void setMouseY(int mouseY);
```

---

### SceneObject

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.SceneObject → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.SceneObject → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.SceneObject → org.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.SceneObject → org.dreambot.api.wrappers.interactive.SceneObject  

**Methods:**
```java
int getFlags();
int getGridX();
int getGridY();
long getIndex();
int getOrientation();
int getPlane();
int getRelativeX();
int getRelativeY();
int getType();
```

| Method | Description |
|--------|-------------|
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |
| `getRelativeX()` | Deprecated. |
| `getRelativeY()` | Deprecated. |

**Inherited methods:**
- From `GameObject`: `canReach`, `distance`, `distance`, `distance`, `exists`, `getActions`, `getAdjustToTerrain`, `getAlternativeIds`, `getAlternativeIDs`, `getAnimationId`, `getAnimationID`, `getAnimationObjectId`, `getBlockingMask`, `getBrightness`, `getComposite`
  ...and 58 more
- From `Entity`: `canReach`, `equals`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getReference`, `getSurroundingArea`, `hashCode`, `interact`, `interact`, `interact`, `interact`, `interact`, `interactForceLeft`
  ...and 2 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `getRegionId`

---

### Verified

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** interface  

**Methods:**
```java
boolean exists();
```

| Method | Description |
|--------|-------------|
| `exists()` | Determines where or not this object exists in game. |

---

### WallObject

**Package:** `org.dreambot.api.wrappers.interactive`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.WallObject → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.WallObject → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.interactive.GameObjectorg.dreambot.api.wrappers.interactive.WallObject → org.dreambot.api.wrappers.interactive.GameObject → org.dreambot.api.wrappers.interactive.WallObject → org.dreambot.api.wrappers.interactive.WallObject  

**Methods:**
```java
int getFlags();
int getGridX();
int getGridY();
long getIndex();
int getOrientation();
int getPlane();
```

| Method | Description |
|--------|-------------|
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |

**Inherited methods:**
- From `GameObject`: `canReach`, `distance`, `distance`, `distance`, `exists`, `getActions`, `getAdjustToTerrain`, `getAlternativeIds`, `getAlternativeIDs`, `getAnimationId`, `getAnimationID`, `getAnimationObjectId`, `getBlockingMask`, `getBrightness`, `getComposite`
  ...and 58 more
- From `Entity`: `canReach`, `equals`, `getBoundingBox`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getReference`, `getSurroundingArea`, `hashCode`, `interact`, `interact`, `interact`, `interact`, `interact`, `interactForceLeft`
  ...and 2 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `getRegionId`

---

# DreamBot API — randoms

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### BankPinConfirmSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.BankPinConfirmSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.BankPinConfirmSolver → org.dreambot.api.randoms.BankPinConfirmSolver  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### BankPinSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.BankPinSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.BankPinSolver → org.dreambot.api.randoms.BankPinSolver  

**Methods:**
```java
int onLoop();
void onStart();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `onStart()` | Runs once when the random solver first activates; This method can be optionally overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `setForceDisable`

---

### BreakSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.BreakSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.BreakSolver → org.dreambot.api.randoms.BreakSolver  

**Methods:**
```java
boolean isBreakRunning();
void onClick(Point point);
int onLoop();
void onPaint(java.awt.Graphics graphics);
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `onPaint(java.awt.Graphics graphics)` | Handles the graphic for the script. |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onStart`, `setForceDisable`
- From `PaintListener`: `onPaint`

---

### DeathsDoor

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.DeathsDoor → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.DeathsDoor → org.dreambot.api.randoms.DeathsDoor  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### DismissBankNotificationTutorial

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.DismissBankNotificationTutorial → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.DismissBankNotificationTutorial → org.dreambot.api.randoms.DismissBankNotificationTutorial  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### DismissBankTutorial

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.DismissBankTutorial → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.DismissBankTutorial → org.dreambot.api.randoms.DismissBankTutorial  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### DismissSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.DismissSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.DismissSolver → org.dreambot.api.randoms.DismissSolver  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### GameLayoutSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.GameLayoutSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.GameLayoutSolver → org.dreambot.api.randoms.GameLayoutSolver  

**Methods:**
```java
int onLoop();
void reset();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### GenieSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.GenieSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.GenieSolver → org.dreambot.api.randoms.GenieSolver  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### LoginSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.LoginSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.LoginSolver → org.dreambot.api.randoms.LoginSolver  

**Methods:**
```java
String getDebugInfo();
void onFinish();
int onLoop();
void onStart();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `getDebugInfo()` | This will return state information about the solver useful for debugging |
| `onFinish()` | Runs once at the end (when shouldExecute first returns false); This method can be optionally overridden |
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `onStart()` | Runs once when the random solver first activates; This method can be optionally overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onPaint`, `onPaint`, `setForceDisable`

---

### RandomEvent

**Package:** `org.dreambot.api.randoms`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<RandomEvent>org.dreambot.api.randoms.RandomEvent → java.lang.Enum<RandomEvent> → org.dreambot.api.randoms.RandomEvent → org.dreambot.api.randoms.RandomEvent  

**Constants:**
```java
BANK_NOTIFICATION
BANK_PIN
BANK_PIN_CONFIRM
BANK_TUTORIAL
BREAK
DEATHS_DOOR
DISMISS
GAME_LAYOUT
GENIE
LOGIN
RESIZABLE_DISABLER
ROOF_DISABLER
WELCOME_SCREEN
ZOOM_SOLVER
```

**Methods:**
```java
String toString();
static RandomEvent valueOf(String name);
static RandomEvent [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### RandomManager

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomManager → org.dreambot.api.randoms.RandomManager  

**Methods:**
```java
void clearRegisteredSolvers();
void disableSolver(String event);
void disableSolver(RandomEvent event);
void enableSolver(String event);
void enableSolver(RandomEvent event);
BreakSolver getBreakSolver();
RandomSolver getCurrentSolver();
java.lang.Thread getRandomThread();
RandomSolver getSolver(String eventName);
WelcomeScreenSolver getWelcomeScreenSolver();
boolean isSolving();
boolean isUsingCustomBreakSolver();
void kill();
int onLoop();
void registerSolver(RandomSolver solver);
void run();
void runRandomManager();
void setRandomThread(java.lang.Thread randomThread);
void unregisterSolver(String event);
void unregisterSolver(RandomEvent event);
```

| Method | Description |
|--------|-------------|
| `getSolver(String eventName)` | Gets a random solver where the random solver's RandomSolver.getEventString() matches the given eventName. |
| `onLoop()` | Does the login for checking and running a random solver |
| `registerSolver(RandomSolver solver)` | Registers a random solver with priority higher than default. |
| `unregisterSolver(String event)` | Removes a random solvers with a given event string. |
| `unregisterSolver(RandomEvent event)` | Removes a random solver. |

---

### RandomManager.State

**Package:** `org.dreambot.api.randoms`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<RandomManager.State>org.dreambot.api.randoms.RandomManager.State → java.lang.Enum<RandomManager.State> → org.dreambot.api.randoms.RandomManager.State → org.dreambot.api.randoms.RandomManager.State  

**Constants:**
```java
RESET
STARTED
```

**Methods:**
```java
static RandomManager.State valueOf(String name);
static RandomManager.State [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### RandomSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.RandomSolver  

**Methods:**
```java
void disable();
void enable();
String getDebugInfo();
String getEventString();
int getMinimumRest();
boolean isEnabled();
boolean isForceDisable();
long lastRan();
void onFinish();
abstract int onLoop();
void onPaint(java.awt.Graphics graphics);
void onPaint(Graphics2D graphics);
void onStart();
void setForceDisable(boolean forceDisable);
abstract boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `getDebugInfo()` | This will return state information about the solver useful for debugging |
| `onFinish()` | Runs once at the end (when shouldExecute first returns false); This method can be optionally overridden |
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `onPaint(java.awt.Graphics graphics)` | Handles the graphic for the script. |
| `onPaint(Graphics2D graphics)` | Handles the graphic for the script. |
| `onStart()` | Runs once when the random solver first activates; This method can be optionally overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

---

### RoofDisabler

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.RoofDisabler → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.RoofDisabler → org.dreambot.api.randoms.RoofDisabler  

**Methods:**
```java
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getDebugInfo`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

### WelcomeScreenSolver

**Package:** `org.dreambot.api.randoms`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.randoms.RandomSolverorg.dreambot.api.randoms.WelcomeScreenSolver → org.dreambot.api.randoms.RandomSolver → org.dreambot.api.randoms.WelcomeScreenSolver → org.dreambot.api.randoms.WelcomeScreenSolver  

**Methods:**
```java
String getDebugInfo();
int onLoop();
boolean shouldExecute();
```

| Method | Description |
|--------|-------------|
| `getDebugInfo()` | This will return state information about the solver useful for debugging |
| `onLoop()` | Runs as long as shouldExecute returns true; This method must be overridden |
| `shouldExecute()` | Checks whether the random even solver should execute; This method must be overridden |

**Inherited methods:**
- From `RandomSolver`: `disable`, `enable`, `getEventString`, `getMinimumRest`, `isEnabled`, `isForceDisable`, `lastRan`, `onFinish`, `onPaint`, `onPaint`, `onStart`, `setForceDisable`

---

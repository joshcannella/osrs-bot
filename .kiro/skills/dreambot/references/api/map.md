# DreamBot API — map

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Area

**Package:** `org.dreambot.api.methods.map`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.map.Area → org.dreambot.api.methods.map.Area  

**Methods:**
```java
boolean contains(int x, int y);
boolean contains(Tile tile);
boolean contains(Entity entity);
static Area generateArea(int radius, Tile center);
List<Tile> getBoundaryPoints();
Rectangle getBoundingBox();
Tile getCenter();
Tile getNearestTile(Entity entity);
Polygon getPolygonArea();
Tile getRandomTile();
Tile getTile();
Tile [] getTiles();
void setZ(int plane);
Area withArea(Area area);
```

| Method | Description |
|--------|-------------|
| `contains(int x, int y)` | Checks if the Area contains the x and the y points |
| `contains(Tile tile)` | Checks if the Area contains the provided Tile |
| `contains(Entity entity)` | Checks if an Area contains the provided Entity |
| `getBoundaryPoints()` | Gets the boundary Tiles, or a list of the area's defined points |
| `getBoundingBox()` | Gets the bounding box of the area |
| `getCenter()` | Gets the center Tile of the Area |
| `getNearestTile(Entity entity)` | Gets the closest Tile in the Area to the provided entity |
| `getPolygonArea()` | Gets the polygon shape of the Area |
| `getRandomTile()` | Gets a random Tile inside the Area |
| `getTile()` | Gets tile of the object. |
| `getTiles()` | Gets all of the tiles contained in the Area |
| `withArea(Area area)` | Creates a new Area containing the tiles from this area and the given area |

**Inherited methods:**
- From `Locatable`: `canReach`, `canReach`, `distance`, `getGridX`, `getGridY`, `getRegionId`, `getTileReference`, `getX`, `getY`, `getZ`, `tileDistance`, `walkingDistance`

---

### Map

**Package:** `org.dreambot.api.methods.map`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.map.Map → org.dreambot.api.methods.map.Map  

**Methods:**
```java
static boolean canReach(Tile destination);
static boolean canReach(Tile destination, boolean offsetDestination);
static boolean canReach(Tile start, Tile destination);
static boolean canReach(Tile start, Tile destination, boolean offsetDestination);
static boolean canReach(Entity destination);
static void clearGameObjectCache();
static int costToTile(int startX, int startY, int destinationX, int destinationY, boolean offsetDestination);
static int costToTile(int startX, int startY, int destinationX, int destinationY, int z, boolean offsetDestination);
static int exactDistance(Tile destination);
static int exactDistance(Tile destination, boolean offsetDestination);
static int exactDistance(Tile start, Tile destination);
static int exactDistance(Tile start, Tile destination, boolean offsetDestination);
static int exactDistance(Entity destination);
static Rectangle getBounds(Tile tile);
static int getFlag(Tile tile);
static Tile getGridLocation(Tile tile);
static int getGridX(Tile tile);
static int getGridY(Tile tile);
static MiniMapTileDestination getMiniMapDestination(Tile tile);
static java.awt.Shape getMinimapShape();
static java.awt.Shape getMinimapShape(int diameterIncrease);
static GameObject [] getObjectsAtTile(Tile tile);
static GameObject [] getObjectsAtTile(Tile tile, boolean cachedResults);
static Polygon getPolygon(Tile tile);
static TileReference [][] getTileGrid(int plane);
static int getTileHeight(Tile tile);
static TileReference getTileReference(int localX, int localY, int z);
static TileReference getTileReference(Tile tile);
static Point[] getVertices(Tile tile);
static Tile getWalkable(Tile tile);
static boolean hoveringMinimap();
static boolean interact(Tile tile);
static boolean interact(Tile tile, String action);
static boolean isClickable(Tile tile);
static boolean isLocal(Tile tile);
static boolean isTileOnMap(Tile tile);
static boolean isTileOnScreen(Tile tile);
static boolean isVisible(Tile tile);
static void reset();
static Point tileToMiniMap(Tile tile);
static Point tileToScreen(Tile tile);
```

| Method | Description |
|--------|-------------|
| `canReach(Tile destination)` | Determines if the given destination tile is reachable from your current local players position, and not blocked by any objects/obstacles. |
| `canReach(Tile destination, boolean offsetDestination)` | Determines if the given destination tile reachable from your current local players position, and not blocked by any objects/obstacles. |
| `canReach(Tile start, Tile destination)` | Determines if the given destination tile reachable from the given start tile, and not blocked by any objects/obstacles. |
| `canReach(Tile start, Tile destination, boolean offsetDestination)` | Determines if the given destination tile reachable from the given start tile, and not blocked by any objects/obstacles. |
| `canReach(Entity destination)` | Determines if the given destination Entity is reachable from your current local players position, and not blocked by any objects/obstacles. |
| `exactDistance(Tile destination)` | Calculates the exact distance (number of tiles) your current player is away from destination. |
| `exactDistance(Tile destination, boolean offsetDestination)` | Determines if the given destination tile reachable from your current local player's server tile, and not blocked by any objects/obstacles. |
| `exactDistance(Tile start, Tile destination)` | Determines if the given destination tile reachable from the given start tile, and not blocked by any objects/obstacles. |
| `exactDistance(Tile start, Tile destination, boolean offsetDestination)` | Determines if the given destination tile reachable from the given start tile, and not blocked by any objects/obstacles. |
| `exactDistance(Entity destination)` | Calculates the exact distance (number of tiles) your current player is away from destination. |
| `getBounds(Tile tile)` | Gets the on-screen rectangle bounds of the given tile. |
| `getFlag(Tile tile)` | Gets collision flag for the given tile. |
| `getGridLocation(Tile tile)` | Gets the grid location of the tile. |
| `getGridX(Tile tile)` | Gets the x location of a tile on the region grid. |
| `getGridY(Tile tile)` | Gets the grid y value of a tile |
| `getMiniMapDestination(Tile tile)` | Gets the mini map Destination for the given tile. |
| `getMinimapShape()` | Gets a generated shape reference for the minimap widget. |
| `getObjectsAtTile(Tile tile)` | Gets the GameObject s at the given tile. |
| `getObjectsAtTile(Tile tile, boolean cachedResults)` | Gets the GameObject s at the given tile. |
| `getPolygon(Tile tile)` | Gets the on-screen polygon of the given tile. |
| `getTileGrid(int plane)` | Get the reference of the given tile object. |
| `getTileHeight(Tile tile)` | Gets the height of a tile. |
| `getTileReference(Tile tile)` | Get the reference of the given tile object. |
| `getVertices(Tile tile)` | Gets an array of points which represent the bounds of a tile. |
| `getWalkable(Tile tile)` | Gets a walkable tile which is closest the given tile. |
| `interact(Tile tile)` | Interacts with the given tile on the map by left-clicking. |
| `interact(Tile tile, String action)` | Interacts with the given tile on the map using the given action. |
| `isClickable(Tile tile)` | Checks if you can click this tile either on screen or on minimap. |
| `isLocal(Tile tile)` | Determines if given tile is local. |
| `isTileOnMap(Tile tile)` | Determines if the given tile is current visible on the mini map. |
| `isTileOnScreen(Tile tile)` | Is tile on screen. |
| `isVisible(Tile tile)` | Checks if the Tile is visible on the main screen. |
| `tileToMiniMap(Tile tile)` | Gets the given tile location on the mini map. |
| `tileToScreen(Tile tile)` | Gets the current screen position of the given tile. |

---

### Tile

**Package:** `org.dreambot.api.methods.map`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.map.Tile → org.dreambot.api.methods.map.Tile  

**Methods:**
```java
Tile clone();
double distance();
double distance(Tile tile);
double distance(Entity entity);
boolean equals(Object obj);
Area getArea(int radius);
int getGridX();
int getGridY();
Polygon getPolygon();
Tile getRandomized();
Tile getRandomized(int deviation);
Tile getTile();
TileReference getTileReference();
int getX();
int getY();
int getZ();
int hashCode();
void setX(int x);
void setY(int y);
void setZ(int z);
String toString();
Tile translate(int x, int y);
Tile translate(Tile tile);
double walkingDistance(Tile tile);
```

| Method | Description |
|--------|-------------|
| `distance()` | Gets the distance between the current Tile and your player. |
| `distance(Tile tile)` | Gets the distance between the current Tile and specified Tile |
| `distance(Entity entity)` | Gets the distance of the current Tile to the Entity |
| `getArea(int radius)` | Creates a square area from this tile with a given radius. |
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |
| `getPolygon()` | Gets the onscreen polygon for this tile |
| `getRandomized()` | Gets a new instance of a random tile with a deviation of a guassian random with a mean of 2 and a sigma of 1 |
| `getRandomized(int deviation)` | Gets a new instance of a random tile within a given deviation. |
| `getTile()` | Gets tile of the object. |
| `getTileReference()` | Gets tile reference of the given object. |
| `getX()` | Gets the X coordinate of the Tile |
| `getY()` | Gets the Y coordinate of the Tile |
| `getZ()` | Gets the Z coordinate of the tile |
| `setX(int x)` | Sets the X coordinate for a Tile |
| `setY(int y)` | Sets the Y coordinate of the Tile |
| `setZ(int z)` | Sets the Z coordinate of the tile |
| `translate(int x, int y)` | Translates this Tile reference's location by the specified amount and returns itself |
| `translate(Tile tile)` | Translates this Tile's location by the specified amount and returns then this Tile with the updated location. |
| `walkingDistance(Tile tile)` | Walking distance based on pathfinding to the given tile. |

**Inherited methods:**
- From `Locatable`: `canReach`, `canReach`, `getRegionId`, `tileDistance`

---

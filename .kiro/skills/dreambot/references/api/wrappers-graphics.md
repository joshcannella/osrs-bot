# DreamBot API — wrappers-graphics

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### GraphicsObject

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.cache.nodes.NodeWrapperorg.dreambot.api.wrappers.cache.nodes.RenderableNodeorg.dreambot.api.wrappers.graphics.GraphicsObject → org.dreambot.api.wrappers.cache.nodes.NodeWrapper → org.dreambot.api.wrappers.cache.nodes.RenderableNodeorg.dreambot.api.wrappers.graphics.GraphicsObject → org.dreambot.api.wrappers.cache.nodes.RenderableNode → org.dreambot.api.wrappers.graphics.GraphicsObject → org.dreambot.api.wrappers.graphics.GraphicsObject  

**Methods:**
```java
boolean equals(Object obj);
int getId();
int getID();
int getSpawnTime();
Tile getTile();
int getX();
int getY();
int getZ();
int hashCode();
String toString();
```

| Method | Description |
|--------|-------------|
| `getID()` | Deprecated. |
| `getTile()` | Gets tile of the object. |
| `getX()` | Gets X coordinate of the given object. |
| `getY()` | Gets Y coordinate of the given object. |
| `getZ()` | Gets Z coordinate of the given object. |

**Inherited methods:**
- From `RenderableNode`: `getHeight`
- From `NodeWrapper`: `getReference`
- From `Locatable`: `canReach`, `canReach`, `distance`, `getGridX`, `getGridY`, `getRegionId`, `getTileReference`, `tileDistance`, `walkingDistance`

---

### Hitsplat

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.graphics.Hitsplat → org.dreambot.api.wrappers.graphics.Hitsplat  

**Methods:**
```java
int getAmount();
int getEndCycle();
int getId();
int getType();
```

---

### ModelWrapper

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.cache.nodes.NodeWrapperorg.dreambot.api.wrappers.cache.nodes.RenderableNodeorg.dreambot.api.wrappers.graphics.ModelWrapper → org.dreambot.api.wrappers.cache.nodes.NodeWrapper → org.dreambot.api.wrappers.cache.nodes.RenderableNodeorg.dreambot.api.wrappers.graphics.ModelWrapper → org.dreambot.api.wrappers.cache.nodes.RenderableNode → org.dreambot.api.wrappers.graphics.ModelWrapper → org.dreambot.api.wrappers.graphics.ModelWrapper  

**Methods:**
```java
Entity getAnimable();
int getIndicesLength();
int[] getIndicesX();
int[] getIndicesY();
int[] getIndicesZ();
byte getOverrideAmount();
byte getOverrideHue();
byte getOverrideLuminance();
byte getOverrideSaturation();
int getVerticesCount();
int[] getVerticesX();
int[] getVerticesY();
int[] getVerticesZ();
void setAnimable(Entity e);
```

**Inherited methods:**
- From `RenderableNode`: `getHeight`
- From `NodeWrapper`: `getReference`

---

### NpcOverride

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.graphics.NpcOverride → org.dreambot.api.wrappers.graphics.NpcOverride  

**Methods:**
```java
short[] getColors();
long getId();
int[] getModelIds();
short[] getTextures();
boolean isUseLocalPlayer();
```

---

### Projectile

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.cache.nodes.NodeWrapperorg.dreambot.api.wrappers.cache.nodes.RenderableNodeorg.dreambot.api.wrappers.graphics.Projectile → org.dreambot.api.wrappers.cache.nodes.NodeWrapper → org.dreambot.api.wrappers.cache.nodes.RenderableNodeorg.dreambot.api.wrappers.graphics.Projectile → org.dreambot.api.wrappers.cache.nodes.RenderableNode → org.dreambot.api.wrappers.graphics.Projectile → org.dreambot.api.wrappers.graphics.Projectile  

**Methods:**
```java
boolean exists();
int getEndCycle();
int getHeight();
double getHeightOffset();
int getId();
int getID();
int getLocalOriginX();
int getLocalOriginY();
int getLocalX();
int getLocalY();
Entity getOriginEntity();
int getOriginEntityIndex();
Tile getOriginTile();
int getOriginX();
int getOriginY();
int getRawOriginX();
int getRawOriginY();
double getRotationX();
double getRotationY();
AnimationSequenceWrapper getSequence();
int getSlope();
double getSpeedX();
double getSpeedY();
double getSpeedZ();
int getStartCycle();
int getTargetDistance();
Entity getTargetEntity();
int getTargetIndex();
Tile getTargetTile();
Tile getTile();
int getX();
int getY();
int getZ();
boolean isMoving();
String toString();
```

| Method | Description |
|--------|-------------|
| `getHeightOffset()` | Deprecated. |
| `getID()` | Deprecated. |
| `getRotationX()` | Deprecated. |
| `getRotationY()` | Deprecated. |
| `getSequence()` | Deprecated. |
| `getSpeedX()` | Deprecated. |
| `getSpeedY()` | Deprecated. |
| `getSpeedZ()` | Deprecated. |
| `getTile()` | Gets tile of the object. |
| `getX()` | Gets X coordinate of the given object. |
| `getY()` | Gets Y coordinate of the given object. |
| `getZ()` | Gets Z coordinate of the given object. |
| `isMoving()` | Deprecated. was only set to true when move method was called, otherwise it was false on construction |

**Inherited methods:**
- From `NodeWrapper`: `getReference`
- From `Locatable`: `canReach`, `canReach`, `distance`, `getGridX`, `getGridY`, `getRegionId`, `getTileReference`, `tileDistance`, `walkingDistance`

---

### SpotAnimation

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.graphics.SpotAnimation → org.dreambot.api.wrappers.graphics.SpotAnimation  

**Methods:**
```java
int getAnimationId();
int getCycle();
int getDelay();
int getTick();
```

| Method | Description |
|--------|-------------|
| `getCycle()` | Gets the cycle of the spot animation |
| `getTick()` | Deprecated. |

---

### Viewport

**Package:** `org.dreambot.api.wrappers.graphics`  
**Type:** interface  

**Methods:**
```java
boolean isOnGameScreen(Point point);
Point localTileToScreen(Tile tile);
Point tileToScreen(int tileX, int tileY, double offsetX, double offsetY, int tileHeight);
Point tileToScreen(int tileX, int tileY, int tileHeight);
Point tileToScreen(Tile tile);
```

| Method | Description |
|--------|-------------|
| `isOnGameScreen(Point point)` | Determines if point is on the game screen. |
| `localTileToScreen(Tile tile)` | Local tile to offset screen position. |
| `tileToScreen(int tileX, int tileY, double offsetX, double offsetY, int tileHeight)` | Tile to offset screen position. |
| `tileToScreen(int tileX, int tileY, int tileHeight)` | Converts offset set of local coordinates into their screen positions |
| `tileToScreen(Tile tile)` | World tile to offset regional tile and converts that regional tile into offset position on the game screen. |

---

# DreamBot API — prayer

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Prayer

**Package:** `org.dreambot.api.methods.prayer`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<Prayer>org.dreambot.api.methods.prayer.Prayer → java.lang.Enum<Prayer> → org.dreambot.api.methods.prayer.Prayer → org.dreambot.api.methods.prayer.Prayer  

**Constants:**
```java
AUGURY
BURST_OF_STRENGTH
CHIVALRY
CLARITY_OF_THOUGHT
DEADEYE
EAGLE_EYE
HAWK_EYE
IMPROVED_REFLEXES
INCREDIBLE_REFLEXES
MYSTIC_LORE
MYSTIC_MIGHT
MYSTIC_VIGOUR
MYSTIC_WILL
PIETY
PRESERVE
PROTECT_FROM_MAGIC
PROTECT_FROM_MELEE
PROTECT_FROM_MISSILES
PROTECT_ITEM
RAPID_HEAL
RAPID_RESTORE
REDEMPTION
RETRIBUTION
RIGOUR
ROCK_SKIN
SHARP_EYE
SMITE
STEEL_SKIN
SUPERHUMAN_STRENGTH
THICK_SKIN
ULTIMATE_STRENGTH
```

**Methods:**
```java
int getChildIndex();
int getLevel();
int getQuickPrayerChild();
int getSetting();
WidgetChild getWidgetChild();
int getWidgetIndex();
boolean isUnlocked();
static Prayer of(String name);
String toString();
static Prayer valueOf(String name);
static Prayer [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### Prayers

**Package:** `org.dreambot.api.methods.prayer`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.prayer.Prayers → org.dreambot.api.methods.prayer.Prayers  

**Methods:**
```java
static boolean flick(Prayer prayer, int delay);
static Prayer [] getActive();
static List<Prayer> getQuickPrayers();
static WidgetChild getWidgetChild(Prayer prayer);
static WidgetChild getWidgetChildQuickPrayer(Prayer prayer);
static boolean isActive(Prayer prayer);
static boolean isOpen();
static boolean isPrayerFilteringEnabled();
static boolean isQuickPrayerActive();
static boolean openTab();
static boolean setPrayerFilteringEnabled(boolean enabled);
static boolean setupQuickPrayers(Prayer ... prayers);
static boolean toggle(boolean active, Prayer prayer);
static boolean toggleQuickPrayer(boolean active);
```

| Method | Description |
|--------|-------------|
| `flick(Prayer prayer, int delay)` | Flicks prayer |
| `getQuickPrayers()` | Returns a list of all selected quick prayers |
| `isActive(Prayer prayer)` | Checks if the provided prayer is active |
| `isPrayerFilteringEnabled()` | This is used to check if prayer filtering is enabled. |
| `openTab()` | Opens the prayer Tab |
| `setPrayerFilteringEnabled(boolean enabled)` | Either enables or disables the prayer filtering options in the Prayer tab |
| `setupQuickPrayers(Prayer ... prayers)` | This will set up your quick prayers to enable only the given Prayer 's, disabling any others currently enabled. |
| `toggle(boolean active, Prayer prayer)` | Toggles prayer |
| `toggleQuickPrayer(boolean active)` | Toggles quick prayer |

---

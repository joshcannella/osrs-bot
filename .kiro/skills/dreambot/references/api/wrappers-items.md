# DreamBot API — wrappers-items

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### GroundItem

**Package:** `org.dreambot.api.wrappers.items`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.interactive.interact.Interactableorg.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.items.GroundItem → org.dreambot.api.wrappers.interactive.interact.Interactable → org.dreambot.api.wrappers.interactive.Entityorg.dreambot.api.wrappers.items.GroundItem → org.dreambot.api.wrappers.interactive.Entity → org.dreambot.api.wrappers.items.GroundItem → org.dreambot.api.wrappers.items.GroundItem  

**Methods:**
```java
boolean exists();
String[] getActions();
int getAmount();
Rectangle getBoundingBox();
int getDespawnTime();
int getGridX();
int getGridY();
int getHeight();
int getId();
Item getItem();
int getLocalX();
int getLocalY();
String getName();
int getOwnership();
int getPlane();
int getRenderableHeight();
Tile getTile();
int getVisibleTime();
int getWorldViewId();
int getX();
int getY();
int getZ();
boolean hasAction(String... actions);
boolean isOnScreen();
void setTile(Tile t);
void setZ(int z);
String toString();
```

| Method | Description |
|--------|-------------|
| `exists()` | Determines where or not this object exists in game. |
| `getBoundingBox()` | Gets the bounding box for the entity's model |
| `getGridX()` | Gets grid (local) X coordinate of the given object. |
| `getGridY()` | Gets grid (local) Y coordinate of the given object. |
| `getId()` | Gets ID number of the given object. |
| `getName()` | Gets the name of the entity |
| `getOwnership()` | Returns the item's ownership value |
| `getPlane()` | Deprecated. see getZ() |
| `getTile()` | Gets tile of the object. |
| `getX()` | Gets X coordinate of the given object. |
| `getY()` | Gets Y coordinate of the given object. |
| `getZ()` | Gets Z coordinate of the given object. |
| `hasAction(String... actions)` | Checks whether given actions are in the list of menu actions for an entity |
| `isOnScreen()` | Checks whether the entity is visible on the game screen |
| `setTile(Tile t)` | Deprecated. |
| `setZ(int z)` | Deprecated. |

**Inherited methods:**
- From `Entity`: `canReach`, `distance`, `distance`, `distance`, `equals`, `getCenterPoint`, `getClickablePoint`, `getLeftClickAction`, `getModel`, `getReference`, `getSurroundingArea`, `getTileReference`, `hashCode`, `interact`, `interact`
  ...and 6 more
- From `Interactable`: `hover`, `rightClick`
- From `Identifiable`: `getID`
- From `Locatable`: `canReach`, `getRegionId`, `tileDistance`

---

### Item

**Package:** `org.dreambot.api.wrappers.items`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.items.Item → org.dreambot.api.wrappers.items.Item  

**Methods:**
```java
boolean equals(Object obj);
String[] getActions();
int getAmount();
org.dreambot.internal.cache.wrappers.def.ItemDefinition getComposite();
ContainerType getContainerType();
AbstractMouseDestination getDestination();
int getDiscardableId();
int getDiscardableID();
int getDiscardableValue();
int getEquippedModelFemale1();
int getEquippedModelFemale2();
int getEquippedModelFemale3();
int getEquippedModelFemaleDialogue1();
int getEquippedModelFemaleDialogue2();
int getEquippedModelFemaleTranslationY();
int getEquippedModelMale1();
int getEquippedModelMale2();
int getEquippedModelMale3();
int getEquippedModelMaleDialog1();
int getEquippedModelMaleDialog2();
int getEquippedModelMaleDialogue1();
int getEquippedModelMaleDialogue2();
int getEquippedModelMaleTranslationY();
String[] getGroundActions();
int getHighAlchValue();
int getId();
BufferedImage getImage();
BufferedImage getImageWithAmount();
int getInventoryModelId();
int getInventoryModelID();
int getLightIntensity();
int getLightMagnitude();
int getLivePrice();
int getLowAlchValue();
short[] getModelRecolorOriginal();
short[] getModelRecolorTarget();
int getModelScaleX();
int getModelScaleY();
int getModelScaleZ();
String getName();
int getNotedItemId();
int getNotedItemID();
Map<Integer,Object> getParams();
int getPlaceholderId();
int getPlaceholderID();
int getPlaceholderValue();
short[] getReTextureToFind();
short[] getReTextureToReplace();
int getRotationLength();
int getRotationX();
int getRotationY();
int getRotationZ();
int getShiftClickIndex();
int getSlot();
int[] getStackVariantId();
int[] getStackVariantID();
int[] getStackVariantSize();
int[] getSubActionIndex(String... actions);
int getTeam();
int getTranslateX();
int getTranslateY();
int getUnnotedItemId();
int getUnnotedItemID();
int getValue();
boolean hasAction(String... actions);
boolean hasDefinition();
int hashCode();
boolean hasSubAction(String... actions);
boolean interact();
boolean interact(String action);
boolean isMembersOnly();
boolean isNotable();
boolean isNoted();
boolean isPlaceholder();
boolean isStackable();
boolean isTradable();
boolean isValid();
void setAmount(int amount);
void setComposite(org.dreambot.internal.cache.wrappers.def.ItemDefinition composite);
void setDestination(RectangleDestination destination);
void setSlot(int slot);
String toString();
boolean useOn(int id);
boolean useOn(String name);
boolean useOn(Filter <Item> filter);
boolean useOn(Entity entity);
boolean useOn(Item item);
```

| Method | Description |
|--------|-------------|
| `getDestination()` | Gets the RectangleDestination for the Item. |
| `getDiscardableID()` | Deprecated. |
| `getId()` | Gets ID number of the given object. |
| `getInventoryModelID()` | Deprecated. |
| `getLivePrice()` | This will get the average selling price on the GE, if available |
| `getName()` | Gets the name of the given object. |
| `getNotedItemID()` | Deprecated. |
| `getPlaceholderID()` | Deprecated. |
| `getSlot()` | For Inventory items only! |
| `getStackVariantID()` | Deprecated. |
| `getSubActionIndex(String... actions)` | Gets the first parent index for a sub action found |
| `getUnnotedItemID()` | Deprecated. |
| `getValue()` | Base item value determined by Jagex, high/low alch is based on this value. |
| `interact()` | see interact(String) with action of null |
| `interact(String action)` | Interacts with this item in its respective container using the action given. |
| `isValid()` | Checks to make sure the item has valid id, stack size, and name |
| `setDestination(RectangleDestination destination)` | Manually sets the destination for this item. |
| `useOn(int id)` | Uses this item on another item Note: this item MUST be an inventory item for this method to work |
| `useOn(String name)` | Uses this item on another item Note: this item MUST be an inventory item for this method to work |
| `useOn(Filter <Item> filter)` | Uses this item on another item Note: this item MUST be an inventory item for this method to work |
| `useOn(Entity entity)` | Uses this item on an entity Note: this item MUST be an inventory item for this method to work |
| `useOn(Item item)` | Uses this item on another item Note: both items MUST be inventory items for this method to work |

**Inherited methods:**
- From `Identifiable`: `getID`

---

### ItemLayer

**Package:** `org.dreambot.api.wrappers.items`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.items.ItemLayer → org.dreambot.api.wrappers.items.ItemLayer  

**Methods:**
```java
RenderableNode getBottom();
int getHeight();
RenderableNode getMiddle();
int getPlane();
Tile getTile();
RenderableNode getTop();
int getX();
int getY();
```

| Method | Description |
|--------|-------------|
| `getHeight()` | Gets the height of this ItemLayer. |
| `getPlane()` | Gets the plane of this ItemLayer. |

---

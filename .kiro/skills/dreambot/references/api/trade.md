# DreamBot API — trade

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Trade

**Package:** `org.dreambot.api.methods.trade`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.trade.Trade → org.dreambot.api.methods.trade.Trade  

**Methods:**
```java
static boolean acceptTrade();
static boolean acceptTrade(int tradeScreen);
static boolean addItem(int itemId, int amount);
static boolean addItem(String itemName, int amount);
static boolean addItem(Item i, int amount);
static boolean canAccept();
static boolean close();
static boolean close(int tradeScreen);
static boolean contains(boolean myScreen, int ... itemIds);
static boolean contains(boolean myScreen, int amount, int itemId);
static boolean contains(boolean myScreen, int amount, String itemName);
static boolean contains(boolean myScreen, String... itemNames);
static boolean declineTrade();
static boolean declineTrade(int tradeScreen);
static String getFirstTitle();
static Item getItem(boolean myScreen, int itemId);
static Item getItem(boolean myScreen, String itemName);
static Item [] getItems(boolean mine);
static Item [] getMyItems();
static Item [] getTheirItems();
static String getTradingWith();
static int getValue(boolean myOffer);
static boolean hasAcceptedTrade(TradeUser tradeUser);
static boolean isOpen();
static boolean isOpen(int tradeScreen);
static boolean removeItem(int itemId, int amount);
static boolean removeItem(String itemName, int amount);
static boolean removeItem(Item i, int amount);
static Rectangle slotBounds(int slot);
static boolean tradeWithPlayer(String playerName);
static boolean tradeWithPlayer(Player player);
static boolean verifyTrade(boolean myScreen, Item [] expected);
```

| Method | Description |
|--------|-------------|
| `acceptTrade()` | Accepts the trade based on which screen is currently open |
| `acceptTrade(int tradeScreen)` | Accepts trade based on which screen you're in |
| `addItem(int itemId, int amount)` | Adds an item from your inventory to trade screen. |
| `addItem(String itemName, int amount)` | Adds an item from your inventory to trade screen. |
| `addItem(Item i, int amount)` | Adds an item from your inventory to trade screen. |
| `canAccept()` | Checks if the Accept button is available to click |
| `close()` | Closes screen based on which screen is currently open |
| `close(int tradeScreen)` | Closes trade screen depending on which screen you input |
| `contains(boolean myScreen, int ... itemIds)` | Checks if your or their trade screen contains all the specified item IDs |
| `contains(boolean myScreen, int amount, int itemId)` | Checks if your or their trade screen contains at least specified amount of specified item ID. |
| `contains(boolean myScreen, int amount, String itemName)` | Checks if your or their trade screen contains at least specified amount of specified item name. |
| `contains(boolean myScreen, String... itemNames)` | Checks if your or their trade screen contains all items specified |
| `declineTrade()` | Declines the trade based on which screen is currently open |
| `declineTrade(int tradeScreen)` | Declines trade for screen specified |
| `getFirstTitle()` | Gets the Title of the first trade screen - contains Trading With name |
| `getItem(boolean myScreen, int itemId)` | Returns the item from the specified screen with specified ID |
| `getItem(boolean myScreen, String itemName)` | Returns item specified from the screen with name specified. |
| `getItems(boolean mine)` | Returns an array of items from the trade screen |
| `getMyItems()` | Gets the items in your trade screen |
| `getTheirItems()` | Gets the items in the other player's trade screen |
| `getTradingWith()` | Gets the name of the player you're trading with. |
| `getValue(boolean myOffer)` | Returns the value of the trade offer |
| `hasAcceptedTrade(TradeUser tradeUser)` | Checks if the user specified has accepted the trade |
| `isOpen()` | Checks if trade is open at all |
| `isOpen(int tradeScreen)` | Checks if specified trade screen is open |
| `removeItem(int itemId, int amount)` | Removes an item from the trade screen. |
| `removeItem(String itemName, int amount)` | Removes an item from the trade screen. |
| `removeItem(Item i, int amount)` | Removes an item from the trade screen. |
| `tradeWithPlayer(String playerName)` | Trades with player specified |
| `tradeWithPlayer(Player player)` | Trades with player specified |
| `verifyTrade(boolean myScreen, Item [] expected)` | Simply checks the given array of Items against the array of items in the screen - must be on the second trade screen |

---

### TradeUser

**Package:** `org.dreambot.api.methods.trade`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<TradeUser>org.dreambot.api.methods.trade.TradeUser → java.lang.Enum<TradeUser> → org.dreambot.api.methods.trade.TradeUser → org.dreambot.api.methods.trade.TradeUser  

**Constants:**
```java
THEM
US
```

**Methods:**
```java
static TradeUser valueOf(String name);
static TradeUser [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

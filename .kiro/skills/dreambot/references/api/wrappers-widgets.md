# DreamBot API — wrappers-widgets

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### GameMenu

**Package:** `org.dreambot.api.wrappers.widgets`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.widgets.GameMenu → org.dreambot.api.wrappers.widgets.GameMenu  

**Methods:**
```java
boolean clickAction(String action);
boolean clickAction(String action, String name);
boolean clickAction(String action, String name, int id);
boolean clickAction(String action, Entity entity);
boolean clickIndex(int index);
boolean close();
boolean contains(String action);
boolean contains(String action, Entity entity);
boolean containsEntity(Entity entity);
boolean containsObject(String name);
Rectangle getBounds();
int getCount();
String getDefaultAction();
MenuRow getFirstMenuRow();
int getHeight();
int getIndex(String action);
int getIndex(String action, String object);
int getIndex(String action, String object, int id);
int getIndex(String action, Entity entity);
Rectangle getIndexRectangle(int index);
int getLastHovered();
List<MenuRow> getMenuRows();
GameMenu getMenuWithAction(String action);
int getSubIndex();
List<GameMenu> getSubMenu();
int getWidth();
int getX();
int getY();
boolean hover(int index);
boolean isMouseOnAction(int index);
boolean isMouseOnAction(String action);
boolean isVisible();
boolean mouseOverAction(int index);
boolean mouseOverAction(String action);
boolean open();
```

| Method | Description |
|--------|-------------|
| `clickAction(String action)` | Clicks the menu action given. |
| `clickAction(String action, String name)` | Click action. |
| `clickAction(String action, String name, int id)` | Click action. |
| `clickAction(String action, Entity entity)` | Click action. |
| `clickIndex(int index)` | Click index. |
| `close()` | Removes the current menu from the game interface. |
| `contains(String action)` | Determines if menu contains action (NOT THE ENTITY/OBJECT NAME) in one of the menu rows. |
| `contains(String action, Entity entity)` | Determines if menu contains action (NOT THE ENTITY/OBJECT NAME) in one of the menu rows. |
| `containsObject(String name)` | Determines if menu contains object name (NOT THE ACTION) in one of the menu rows. |
| `getBounds()` | Gets bounds. |
| `getDefaultAction()` | Gets default action. |
| `getIndex(String action)` | Gets the index of offset menu item in the list of menu items |
| `getIndex(String action, String object)` | Gets index. |
| `getIndex(String action, String object, int id)` | Gets the index of offset menu item in the list of menu items using the given action, object, and entity index if provided. |
| `getIndex(String action, Entity entity)` | Gets index. |
| `getIndexRectangle(int index)` | Gets index rectangle. |
| `getMenuRows()` | Gets each row of the menu as a MenuRow object |
| `isMouseOnAction(int index)` | Is mouse on action. |
| `isMouseOnAction(String action)` | Is mouse on action. |
| `mouseOverAction(int index)` | Mouse over action. |
| `mouseOverAction(String action)` | Mouse over action. |

---

### Menu

**Package:** `org.dreambot.api.wrappers.widgets`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.widgets.Menu → org.dreambot.api.wrappers.widgets.Menu  

**Methods:**
```java
static boolean clickAction(String action);
static boolean clickAction(String action, String name);
static boolean clickAction(String action, String name, int id);
static boolean clickAction(String action, Entity entity);
static boolean clickIndex(int index);
static boolean close();
static boolean contains(String action);
static boolean contains(String action, Entity entity);
static boolean containsEntity(Entity entity);
static boolean containsObject(String name);
static Rectangle getBounds();
static int getCount();
static String getDefaultAction();
static MenuRow getFirstMenuRow();
static int getHeight();
static int getIndex(String action);
static int getIndex(String action, String object);
static int getIndex(String action, String object, int id);
static int getIndex(String action, Entity entity);
static Rectangle getIndexRectangle(int index);
static GameMenu getMenu();
static List<MenuRow> getMenuRows();
static List<MenuRow> getMenuRows(org.dreambot.internal.injector.interfaces.RSMenu menu);
static List<GameMenu> getSubMenus();
static int getWidth();
static int getX();
static int getY();
static boolean hover(int index);
static boolean inject(MenuRow row);
static boolean inject(MenuRow row, Point p);
static boolean isMenuManipulationActive();
static boolean isMouseOnAction(int index);
static boolean isMouseOnAction(String action);
static boolean isVisible();
static boolean mouseOverAction(int index);
static boolean mouseOverAction(String action);
static boolean open();
static void toggleMenuManipulation(boolean toggle);
```

| Method | Description |
|--------|-------------|
| `clickAction(String action)` | Clicks the menu action given. |
| `clickAction(String action, String name)` | Click action. |
| `clickAction(String action, String name, int id)` | Click action. |
| `clickAction(String action, Entity entity)` | Click action. |
| `clickIndex(int index)` | Click index. |
| `close()` | Removes the current top level menu from the game interface. |
| `contains(String action)` | Determines if menu contains action (NOT THE ENTITY/OBJECT NAME) in one of the menu rows. |
| `contains(String action, Entity entity)` | Determines if menu contains action (NOT THE ENTITY/OBJECT NAME) in one of the menu rows. |
| `containsObject(String name)` | Determines if menu contains object name (NOT THE ACTION) in one of the menu rows. |
| `getBounds()` | Gets bounds. |
| `getDefaultAction()` | Gets default action. |
| `getIndex(String action)` | Gets the index of offset menu item in the list of menu items |
| `getIndex(String action, String object)` | Gets index. |
| `getIndex(String action, String object, int id)` | Gets the index of offset menu item in the list of menu items using the given action, object, and entity index if provided. |
| `getIndex(String action, Entity entity)` | Gets index. |
| `getIndexRectangle(int index)` | Gets index rectangle. |
| `getMenuRows()` | Gets each row of the menu as a MenuRow object |
| `isMouseOnAction(int index)` | Is mouse on action. |
| `isMouseOnAction(String action)` | Is mouse on action. |
| `mouseOverAction(int index)` | Mouse over action. |
| `mouseOverAction(String action)` | Mouse over action. |

---

### MenuRow

**Package:** `org.dreambot.api.wrappers.widgets`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.widgets.MenuRow → org.dreambot.api.wrappers.widgets.MenuRow  

**Fields:**
```java
static String FORMAT_RS_ARROW
```

**Methods:**
```java
MenuRow clone();
boolean compareAction(String action);
boolean compareEntity(Entity e);
boolean compareObject(String object);
boolean equals(Object o);
String getAction();
int getId();
int getID();
int getIndex();
int getItemIdCode();
String getObject();
int getOpCode();
String getRawAction();
String getRawObject();
int getTargetRegionIndex();
int getXCode();
int getYCode();
int hashCode();
boolean isShift();
void setAction(String action);
void setId(int id);
void setIndex(int index);
void setItemIdCode(int itemIdCode);
void setObject(String object);
void setOpCode(int opCode);
void setShift(boolean shift);
void setTargetRegionIndex(int targetRegionIndex);
void setXCode(int xCode);
void setYCode(int yCode);
String toString();
```

| Method | Description |
|--------|-------------|
| `compareAction(String action)` | Compare action a inputted action, and determines if the action associated with this menu row matches the given action. |
| `compareObject(String object)` | Compares a inputted object, and determines if the object associated with this menu row matches the given object. |
| `getAction()` | -- GETTER -- Gets action associated with this menu row. |
| `getId()` | Gets ID of this menu row. |
| `getID()` | Deprecated. |
| `getObject()` | -- GETTER -- Gets object reference associated with this menu row. |
| `getOpCode()` | -- GETTER -- Get opcode of this menu row. |
| `getXCode()` | Get x code of this menu row. |
| `getYCode()` | Get y code of this menu row. |
| `setAction(String action)` | -- GETTER -- Gets action associated with this menu row. |
| `setObject(String object)` | -- GETTER -- Gets object reference associated with this menu row. |
| `setOpCode(int opCode)` | -- GETTER -- Get opcode of this menu row. |

---

### MenuRowBuilder

**Package:** `org.dreambot.api.wrappers.widgets.builder`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.widgets.builder.MenuRowBuilder → org.dreambot.api.wrappers.widgets.builder.MenuRowBuilder  

**Methods:**
```java
static MenuRow buildForBank(int slot, String action);
static MenuRow buildForInventory(int slot, String action);
static MenuRow buildForSpell(Spell spell);
static MenuRow buildForSpell(Spell spell, String action);
static MenuRow buildForWidget(WidgetChild widget, String action, String object);
static MenuRow buildForWorld(int world, WidgetChild parent);
static MenuRow buildFromEntity(Entity entity, String action);
static MenuRow buildFromGroundItem(GroundItem groundItem, String action);
static MenuRow buildFromNpc(NPC npc, String action);
static MenuRow buildFromObject(GameObject gameObject, String action);
static MenuRow buildFromPlayer(Player player, String action);
static String removeFormat(String action, java.util.regex.Pattern... patterns);
static String removeFormatting(String action);
```

| Method | Description |
|--------|-------------|
| `buildForBank(int slot, String action)` | Builds a MenuRow for a bank withdraw/deposit action |
| `buildForInventory(int slot, String action)` | This is for normal inventory usage, adjusts for bank, GE, and shop |
| `buildForWidget(WidgetChild widget, String action, String object)` | Builds a MenuRow for a given WidgetChild with the given action and object. |
| `buildFromEntity(Entity entity, String action)` | Builds a menu row from a given entity with the provided action. |
| `buildFromGroundItem(GroundItem groundItem, String action)` | Builds a menu row from a ground item with a given action |
| `buildFromNpc(NPC npc, String action)` | Builds MenuRow for given NPC with given action |
| `buildFromObject(GameObject gameObject, String action)` | Builds a MenuRow for a given GameObject and a given Action |
| `buildFromPlayer(Player player, String action)` | Builds a MenuRow from a given Player with a given action |

---

### Message

**Package:** `org.dreambot.api.wrappers.widgets.message`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.widgets.message.Message → org.dreambot.api.wrappers.widgets.message.Message  

**Methods:**
```java
boolean equals(Object obj);
String getClan();
String getMessage();
int getTime();
MessageType getType();
int getTypeID();
String getUsername();
int hashCode();
String toString();
```

| Method | Description |
|--------|-------------|
| `getTypeID()` | Deprecated. |

---

### MessageType

**Package:** `org.dreambot.api.wrappers.widgets.message`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<MessageType>org.dreambot.api.wrappers.widgets.message.MessageType → java.lang.Enum<MessageType> → org.dreambot.api.wrappers.widgets.message.MessageType → org.dreambot.api.wrappers.widgets.message.MessageType  

**Constants:**
```java
AUTO
BROADCAST
CHAT_CHANNEL
CLAN_CHAT
CLAN_CREATION_INVITE
CLAN_GUEST_CHAT
CLAN_GUEST_MESSAGE
CLAN_IRON_MAN_FORM_GROUP
CLAN_IRON_MAN_GROUP_WITH
CLAN_MESSAGE
CLAN_WARS_CHALLENGE
DIALOG
DUEL
ENGINE
FEEDBACK
FILTERED
FRIENDLY_DUEL
FRIENDS_NOTIFICATION
GAME
ITEM_EXAMINE
MESSAGE_BOX
MOD_AUTO
MOD_CHAT
MOD_PRIVATE_CHAT
NEW_FRIEND_NOTIFICATION
NEW_IGNORE_NOTIFICATION
NPC_EXAMINE
OBJECT_EXAMINE
PLAYER
PRIVATE_INFO
PRIVATE_RECV
PRIVATE_SENT
TIMEOUT_MESSAGE
TRADE
TRADE_COMPLETE
TRADE_SENT
WELCOME
```

**Methods:**
```java
int getId();
int getID();
static MessageType getType(int id);
static MessageType valueOf(String name);
static MessageType [] values();
```

| Method | Description |
|--------|-------------|
| `getID()` | Deprecated. |
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### WidgetChild

**Package:** `org.dreambot.api.wrappers.widgets`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.wrappers.cache.nodes.NodeWrapperorg.dreambot.api.wrappers.widgets.WidgetChild → org.dreambot.api.wrappers.cache.nodes.NodeWrapper → org.dreambot.api.wrappers.widgets.WidgetChild → org.dreambot.api.wrappers.widgets.WidgetChild  

**Methods:**
```java
boolean containsMouse();
boolean equals(Object o);
String[] getActions();
int getActionType();
int getBorderThickness();
int getBoundsIndex();
WidgetChild getChild(int id);
int getChildId();
List<Item> getChildItems();
WidgetChild [] getChildren();
int getChildrenCount();
int getChildSequenceId();
int getChildTextureID();
int[] getContentIDs();
int[] getContentStackSizes();
int getContentType();
int getDisabledMediaID();
int getDisabledMediaType();
int getDragDeadTime();
int getEnabledMediaID();
int getEnabledMediaType();
int getGrandChildId();
int getHeight();
int getID();
int getIndex();
Item getItem();
int getItemId();
List<Item> getItems();
int getItemStack();
String getLeftClickAction();
int getLoopCycle();
int getModelId();
int getModelRotationX();
int getModelRotationY();
int getModelRotationZ();
int getModelType();
String getName();
int getParentID();
int getRawId();
int getRealID();
int getRealParentId();
int getRealParentID();
Rectangle getRectangle();
org.dreambot.internal.injector.interfaces.RSWidget getReference();
int getRelativeX();
int getRelativeY();
int getScrollMax();
int getScrollX();
int getScrollY();
String getSelectedAction();
int getSequenceId();
int getSequenceIndex();
int getShadowColor();
WidgetChild getSibling(int offset);
String getSpell();
int getSpriteAngle();
int getSubActionIndex(String action);
String[][] getSubActions();
String getText();
int getTextColor();
int getTextureId();
String getTooltip();
int getType();
int getWidgetId();
int getWidth();
int getX();
int getY();
boolean hasAction(String action);
boolean hasActionExact(String action);
int hashCode();
boolean hasListener();
boolean hasScript();
boolean hasSubAction(String action);
boolean interact();
boolean interact(Filter <String> filter);
boolean interact(String action);
boolean isGrandChild();
boolean isHidden();
boolean isVisible();
void setContainsMouse(boolean contains);
void setContentIDs(int[] ids);
void setContentStackSizes(int[] sizes);
void setDragDeadTime(int time);
void setHasListener(boolean has);
void setHasScript(boolean has);
void setIndex(int index);
void setItemId(int id);
void setItemStack(int stack);
void setText(String text);
String toString();
```

| Method | Description |
|--------|-------------|
| `containsMouse()` | Deprecated. |
| `getActions()` | Gets the Actions of a WidgetChild |
| `getActionType()` | Gets the action type of a WidgetChild |
| `getBorderThickness()` | Deprecated. |
| `getBoundsIndex()` | Deprecated. |
| `getChild(int id)` | Gets a specific WidgetChild of the WidgetChild |
| `getChildId()` | Gets the second level (child) ID of this WidgetChild |
| `getChildItems()` | Gets a List of Items from the WidgetChild's children |
| `getChildren()` | Gets the Children of aWidgetChild |
| `getChildSequenceId()` | Deprecated. |
| `getChildTextureID()` | Deprecated. |
| `getContentIDs()` | Deprecated. |
| `getContentStackSizes()` | Deprecated. |
| `getDisabledMediaID()` | Deprecated. see getModelId() |
| `getDisabledMediaType()` | Deprecated. see getModelType() |
| `getEnabledMediaID()` | Deprecated. see getModelId() |
| `getEnabledMediaType()` | Deprecated. see getModelType() |
| `getGrandChildId()` | Gets the 3rd level (grandchild) ID of the WidgetChild |
| `getHeight()` | Gets the height of a WidgetChild |
| `getID()` | Deprecated. see getChildId() |
| `getIndex()` | Deprecated. see getGrandChildId() |
| `getItem()` | Gets the Item held in the WidgetChild |
| `getItemId()` | Gets the Item ID of the WidgetChild |
| `getItems()` | Deprecated. |
| `getItemStack()` | Gets the item stack size of the WidgetChild |
| `getLoopCycle()` | Gets the Loop Cycle of the WidgetChild |
| `getParentID()` | Deprecated. see getWidgetId() |
| `getRawId()` | Gets the Real ID of aWidgetChild |
| `getRealID()` | Deprecated. see getRawId() |
| `getRealParentID()` | Deprecated. |
| `getRectangle()` | Gets the Rectangle of the WidgetChild |
| `getRelativeX()` | Gets the Relative X location of the WidgetChild |
| `getRelativeY()` | Gets the Relative Y location of a WidgetChild |
| `getScrollMax()` | Gets the scroll max value of the WidgetChild |
| `getScrollX()` | Gets the scrolled X position of the WidgetChild |
| `getScrollY()` | Gets the scrolled Y position of theWidgetChild |
| `getSelectedAction()` | Gets the selected action of the widget. |
| `getSequenceId()` | Gets the Sprite ID of the WidgetChild |
| `getSequenceIndex()` | Deprecated. |
| `getShadowColor()` | Deprecated. |
| `getSibling(int offset)` | This returns the sibling, another WidgetChild with the same parent and grand parent if applicable, offset by the provided offset |
| `getSpell()` | Deprecated. |
| `getText()` | Gets the Text of a WidgetChild |
| `getTextColor()` | Gets the text color of the WidgetChild |
| `getTextureId()` | Gets the Texture ID of the WidgetChild |
| `getTooltip()` | Gets the tooltip of the WidgetChild |
| `getType()` | gets the type value of the WidgetChild |
| `getWidgetId()` | Gets the top level ID of the widget |
| `getWidth()` | Gets the Width of the WidgetChild |
| `getX()` | Gets the X value of the location of the WidgetChild |
| `getY()` | Gets the Y value of the location of the WidgetChild |
| `hasAction(String action)` | Checks to see if any of this widget's actions contains (case-insensitive) the given action |
| `hasActionExact(String action)` | Checks to see if this widget has the full given action |
| `interact()` | Clicks the WidgetChild |
| `interact(Filter <String> filter)` | Interacts with a WidgetChild with the first matching action |
| `interact(String action)` | Interacts with a WidgetChild with a specified action |
| `isGrandChild()` | Checks if the WidgetChild is a grandchild |
| `isHidden()` | Checks if the WidgetChild is hidden |
| `setContainsMouse(boolean contains)` | Deprecated. |
| `setContentIDs(int[] ids)` | Deprecated. |
| `setContentStackSizes(int[] sizes)` | Deprecated. |
| `setHasScript(boolean has)` | Deprecated. |
| `setIndex(int index)` | Deprecated. |

---

# DreamBot API — containers

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Bank

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.impl.bank.Bank → org.dreambot.api.methods.container.impl.bank.Bank  

**Methods:**
```java
static boolean addToBankHistoryCache(Item item);
static List<Item> all();
static List<Item> all(Filter <Item> filter);
static int availableTabs();
static Rectangle calculateSlotPosition(int slot);
static int capacity();
static boolean close();
static boolean contains(int id);
static boolean contains(int[] ids);
static boolean contains(Integer... ids);
static boolean contains(Object o);
static boolean contains(String string);
static boolean contains(String... names);
static boolean contains(Filter <Item> filter);
static boolean containsAll(int[] ids);
static boolean containsAll(Integer... ids);
static boolean containsAll(String... names);
static boolean containsAll(Collection<?> collection);
static int count(int id);
static int count(int[] ids);
static int count(Integer... ids);
static int count(String name);
static int count(String... names);
static int count(BankTab tab);
static int count(Filter <Item> filter);
static boolean deposit(int id);
static boolean deposit(int id, int amount);
static boolean deposit(String name);
static boolean deposit(String name, int amount);
static boolean deposit(Filter <Item> filter);
static boolean deposit(Filter <Item> filter, int amount);
static boolean deposit(Item item);
static boolean deposit(Item item, int amount);
static boolean depositAll(int id);
static boolean depositAll(String name);
static boolean depositAll(Filter <Item> filter);
static boolean depositAll(Item item);
static boolean depositAllEquipment();
static boolean depositAllExcept(int[] ids);
static boolean depositAllExcept(Integer... ids);
static boolean depositAllExcept(String... names);
static boolean depositAllExcept(Filter <Item> filter);
static boolean depositAllItems();
static boolean drag(int fromSlot, int toSlot);
static boolean emptyContainers();
static int emptySlotCount();
static List<Item> except(Filter <Item> filter);
static int fullSlotCount();
static Item get(int id);
static Item get(int[] ids);
static Item get(Integer... ids);
static Item get(String name);
static Item get(String... names);
static Item get(Filter <Item> filter);
static List<Item> getBankHistoryCache();
static WidgetChild getChild(int id);
static WidgetChild getChild(String name);
static WidgetChild getChild(Filter <Item> filter);
static WidgetChild getChild(Item item);
static WidgetChild getChildForSlot(int slot);
static Entity getClosestBank(BankType type);
static BankLocation getClosestBankLocation();
static BankLocation getClosestBankLocation(boolean includeTeleports);
static BankTab getCurrentBankTab();
static int getCurrentTab();
static int getCustomWithdrawAmount();
static BankQuantitySelection getDefaultQuantity();
static int getFirstEmptySlot();
static int getFirstFullSlot();
static int getIdForSlot(int slot);
static Item getItemInSlot(int slot);
static long getLastBankHistoryCacheTick();
static long getLastBankHistoryCacheTime();
static int getLastUpdatedTick();
static String getNameForSlot(int slot);
static BankMode getRearrangeMode();
static int getRow(Item item);
static int getScrollHeight();
static int getSlot(int id);
static int getSlot(String name);
static int getSlot(Filter <Item> filter);
static int getTab(int slot);
static int getTab(Item item);
static BankMode getWithdrawMode();
static boolean isAlwaysOpenTab();
static boolean isCached();
static boolean isEmpty();
static boolean isFull();
static boolean isLoaded();
static boolean isOpen();
static boolean isSlotEmpty(int slot);
static boolean isSlotFull(int slot);
static boolean isSlotVisible(Item item);
static boolean needToScroll(Item item);
static boolean needToScrollDown(Item item);
static boolean needToScrollUp(Item item);
static boolean onlyContains(int[] ids);
static boolean onlyContains(Integer... ids);
static boolean onlyContains(String... names);
static boolean onlyContains(Filter <Item> f);
static boolean open();
static boolean open(BankLocation bank);
static boolean openTab(int tab);
static boolean openTab(BankTab tab);
static boolean placeHoldersEnabled();
static void reset();
static void reset(boolean full);
static void resetCache();
static boolean scroll(int id);
static boolean scroll(int id, BankScroll type);
static boolean scroll(String name);
static boolean scroll(String name, BankScroll type);
static boolean scroll(Filter <Item> filter);
static boolean scroll(Filter <Item> filter, BankScroll type);
static Item set(int index, Item element);
static void setAlwaysOpenTab(boolean alwaysOpenTab);
static boolean setDefaultQuantity(BankQuantitySelection selection);
static boolean setRearrangeMode(BankMode mode);
static void setUseBankHistoryCache(boolean useCache);
static boolean setWithdrawMode(BankMode mode);
static Rectangle slotBounds(int slot);
static Rectangle slotBounds(Item item);
static boolean slotContains(int slot, int[] ids);
static boolean slotContains(int slot, Integer... ids);
static boolean slotContains(int slot, String... names);
static boolean slotContains(int slot, Filter <Item> filter);
static boolean slotContains(int slot, Item t);
static boolean slotNameContains(int slot, String sub);
static int tabContainerWidgetId();
static boolean togglePlaceholders(boolean enabled);
static void update();
static void updateCache();
static void updateCache(Collection<Item> items);
static int widgetChildId();
static int widgetParentId();
static boolean withdraw(int id);
static boolean withdraw(int id, int amount);
static boolean withdraw(Filter <Item> filter);
static boolean withdraw(Filter <Item> filter, int amount);
static boolean withdraw(String name);
static boolean withdraw(String name, int amount);
static boolean withdrawAll(int id);
static boolean withdrawAll(Filter <Item> filter);
static boolean withdrawAll(String name);
```

| Method | Description |
|--------|-------------|
| `addToBankHistoryCache(Item item)` | This allows you to add an item to the bank history cache, it should only be necessary if you're doing things outside our API. |
| `all()` | Gets the items in your bank |
| `all(Filter <Item> filter)` | A list of all the non-null items that match the given filter Filters out null before checking against the filter |
| `availableTabs()` | Gets the number of tabs currently available |
| `calculateSlotPosition(int slot)` | Gets the virtual position for the specified slot. |
| `capacity()` | Gets the bank's max capacity |
| `close()` | Exits the bank with the close button. |
| `contains(int id)` | Checks if the bank contains the given ID Placeholders are ignored |
| `contains(int[] ids)` | See contains(Integer...) |
| `contains(Integer... ids)` | Checks if your collection contains at least one item which meets one of the specified ids. |
| `contains(Object o)` | Checks to see if the bank contains an item matching the given Object Placeholders are ignored |
| `contains(String string)` | Determines if bank contains an item that matches the given name. |
| `contains(String... names)` | Checks if your collection contains at least one item which meets one of the specified names. |
| `contains(Filter <Item> filter)` | Checks if your collection contains at least one item which meets the specified filter. |
| `containsAll(int[] ids)` | see containsAll(Integer...) |
| `containsAll(Integer... ids)` | Checks if your Bank contains all of the items with specified IDs Placeholders are ignored |
| `containsAll(String... names)` | Checks if your Bank contains all of the items with names specified Placeholders are ignored |
| `containsAll(Collection<?> collection)` | Checks if your bank contains all of the items in the collection |
| `count(int id)` | Count of all the items that match the search. |
| `count(int[] ids)` | See count(Integer...) |
| `count(Integer... ids)` | Count of all the items that match the search. |
| `count(String name)` | Count of all the items that match the search. |
| `count(String... names)` | Count of all the items that match the search. |
| `count(BankTab tab)` | Gets the Item count for the BankTab |
| `count(Filter <Item> filter)` | Count of all the items that match the search. |
| `deposit(Filter <Item> filter, int amount)` | Deposits the first item that matches the given Filter for the given amount |
| `depositAllEquipment()` | Deposits all worn equipment with the deposit all equipment button. |
| `depositAllExcept(Filter <Item> filter)` | Deposits all items that do not match the given Filter Will call depositAllItems() if your inventory does not contain any of the given filter (nothing to exclude) |
| `depositAllItems()` | Deposits all items in your inventory with the deposit all button. |
| `drag(int fromSlot, int toSlot)` | Drags the item from one slot to another, either of which can be empty to simply move an item from one slot to the other |
| `emptyContainers()` | Presses the empty containers widget |
| `emptySlotCount()` | Number of slots that do not contain an item or placeholder |
| `except(Filter <Item> filter)` | Get all items which do not fit the given criteria. |
| `fullSlotCount()` | Number of slots that contain non-null items Includes placeholders as a full slot |
| `get(int id)` | Gets the first item with the given id |
| `get(int[] ids)` | See get(Integer...) |
| `get(Integer... ids)` | Gets the first Item that matches one of the given ids |
| `get(String name)` | Get the item for specified name. |
| `get(String... names)` | Gets the first item that matches one of the given names |
| `get(Filter <Item> filter)` | Gets the first Item that matches the given Filter |
| `getBankHistoryCache()` | Gets the bank item history cache. |
| `getChild(int id)` | Gets the WidgetChild of the Item with the given ID |
| `getChild(String name)` | Gets the WidgetChild of the Item with the given name |
| `getChild(Filter <Item> filter)` | Gets the WidgetChild of the Item that matches the given Filter |
| `getChild(Item item)` | Gets the WidgetChild of the Item based on the item's slot value |
| `getChildForSlot(int slot)` | Gets the WidgetChild for the given slot |
| `getClosestBank(BankType type)` | Gets the nearest bank interactable based on BankType |
| `getClosestBankLocation()` | Get the closest bank location. |
| `getClosestBankLocation(boolean includeTeleports)` | Gets the nearest bank location see BankLocation.getNearest(Tile, boolean) |
| `getCurrentBankTab()` | Gets the current BankTab |
| `getCurrentTab()` | Gets the current tab that is open |
| `getDefaultQuantity()` | Gets the currently selected default quantity for withdraw or deposit actions |
| `getFirstEmptySlot()` | Gets the first empty slot in the bank. |
| `getFirstFullSlot()` | Gets the next full slot in your bank |
| `getIdForSlot(int slot)` | Gets the ID of the Item in the slot. |
| `getItemInSlot(int slot)` | Gets item in specified slot. |
| `getLastBankHistoryCacheTick()` | Returns the last game tick (using Client.getGameTick() ) of a cache update |
| `getLastBankHistoryCacheTime()` | Returns the last timestamp (using System#currentTimeMillis) of a cache update |
| `getNameForSlot(int slot)` | Gets the Name for the Item in the slot. |
| `getRearrangeMode()` | Gets the current rearrange mode type; |
| `getRow(Item item)` | Gets the row of the Item |
| `getScrollHeight()` | Determines the current scroll position height of the bank's scroll container. |
| `getSlot(int id)` | Gets the slot for the item with specified ID. |
| `getSlot(String name)` | Gets the slot for the item with specified name. |
| `getSlot(Filter <Item> filter)` | Gets the slot for the first item that matches the given filter |
| `getTab(Item item)` | Gets the tab for the Item |
| `getWithdrawMode()` | Gets the current withdraw BankMode |
| `isCached()` | Checks if the bank has been cached. |
| `isEmpty()` | Checks if the bank is empty (including placeholders) |
| `isFull()` | Determines if bank is full. |
| `isLoaded()` | Checks to see if the Bank container has been loaded successfully at least once Check is done during all() call |
| `isOpen()` | Checks if the bank is currently open or not. |
| `isSlotEmpty(int slot)` | Checks if the given slot contains an item |
| `isSlotFull(int slot)` | Checks if the slot has an Item in it |
| `isSlotVisible(Item item)` | Determines if the given Item 's slot is visible |
| `needToScroll(Item item)` | Determines if the bot needs to scroll to find the Item |
| `needToScrollDown(Item item)` | Determines if it needs to scroll down for the Item to be visible |
| `needToScrollUp(Item item)` | Determines if it needs to scroll up in order to see the given Item |
| `onlyContains(int[] ids)` | See onlyContains(Integer...) |
| `onlyContains(Integer... ids)` | Checks if your Bank only contains items with the specified IDs |
| `onlyContains(String... names)` | Checks if your Bank only contains items with specified names |
| `onlyContains(Filter <Item> f)` | Checks if your Bank only contains items that match the Filter |
| `open()` | Finds the nearest BankType and attempts to open it. |
| `open(BankLocation bank)` | Opens a specific bank location, if you're not in the area it will call walk(Tile) and return false If given a null bank location, it will verify the nearest bank type's location (if we have it) If that verification fails, it will recalculate to find the nearest valid bank location. |
| `openTab(int tab)` | Opens the bank tab specified |
| `openTab(BankTab tab)` | Opens the BankTab specified. |
| `placeHoldersEnabled()` | Checks whether placeholders are currently enabled |
| `resetCache()` | This will reset the bank history cache to it's initial state |
| `scroll(int id)` | Scrolls to the item with the given ID see scroll(int, BankScroll) with a random choice of scroll types |
| `scroll(int id, BankScroll type)` | Scrolls to the item with the given ID using the BankScroll scroll type |
| `scroll(String name)` | Scrolls to the item with the given name see scroll(String, BankScroll) with a random BankScroll type |
| `scroll(String name, BankScroll type)` | Scrolls to the item with the given name using the given BankScroll type |
| `scroll(Filter <Item> filter)` | Scrolls to the item which matches the given filter. |
| `scroll(Filter <Item> filter, BankScroll type)` | Scrolls to the item which matches the given filter using the given BankScroll type |
| `set(int index, Item element)` | Sets an Item to the given slot Used internally, shouldn't really be touched by API users |
| `setDefaultQuantity(BankQuantitySelection selection)` | Sets the BankQuantitySelection for bank actions |
| `setRearrangeMode(BankMode mode)` | Sets the Rearrange mode for the bank either BankMode.SWAP or BankMode.INSERT |
| `setUseBankHistoryCache(boolean useCache)` | Set whether our methods use the Bank cache if you call a method while not in the bank |
| `setWithdrawMode(BankMode mode)` | Sets the withdraw mode, either BankMode.ITEM or BankMode.NOTE |
| `slotBounds(int slot)` | Gets the bounds of a slot |
| `slotBounds(Item item)` | Calls slotBounds(int) with Item.getSlot() If Item is null returns null Rectangle |
| `slotContains(int slot, int[] ids)` | See slotContains(int, Integer...) |
| `slotContains(int slot, Integer... ids)` | Checks if the slot contains an item with an ID matching one of the given ids |
| `slotContains(int slot, String... names)` | Checks if the slot contains items that matches the given names. |
| `slotContains(int slot, Filter <Item> filter)` | Checks if the slot contains an item that matches the filter. |
| `slotContains(int slot, Item t)` | Checks if the slot contains given Item |
| `slotNameContains(int slot, String sub)` | Checks if the slot has an item whose name contains the given sub string |
| `tabContainerWidgetId()` | Gets the ID of the WidgetChild for the tab container |
| `togglePlaceholders(boolean enabled)` | Toggles whether placeholders are enabled or not |
| `updateCache()` | Used for updating the bank cache, generally reserved for internal use but can be called in a script during custom banking This will clear the bank cache and update it with all bank items |
| `updateCache(Collection<Item> items)` | Clears the bank history cache and fills it with the given collection of items, only works if bank is open |
| `widgetChildId()` | Gets the ID of the WidgetChild for the bank interface |
| `widgetParentId()` | Gets the ID of the Widget for the bank |
| `withdraw(int id)` | Withdraws one of the given item id |
| `withdraw(int id, int amount)` | Withdraws an item for the id and amount given. |
| `withdraw(Filter <Item> filter)` | Withdraws one item that matches the given Filter |
| `withdraw(Filter <Item> filter, int amount)` | Withdraws an item that matches the given Filter for the given amount For withdraw All-but-one pass in value: -9411 |
| `withdraw(String name)` | Withdraws one of an item with the given name |
| `withdraw(String name, int amount)` | Withdraws an item for the name and amount given. |
| `withdrawAll(int id)` | Withdraws all of an item for a given ID |
| `withdrawAll(Filter <Item> filter)` | Withdraws all of an item that matches the given Filter |
| `withdrawAll(String name)` | Withdraws all of an item for the given name |

---

### BankLocation

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<BankLocation>org.dreambot.api.methods.container.impl.bank.BankLocation → java.lang.Enum<BankLocation> → org.dreambot.api.methods.container.impl.bank.BankLocation → org.dreambot.api.methods.container.impl.bank.BankLocation  

**Constants:**
```java
AL_KHARID
ALDARIN
ANCIENT_PRISON
ARCEUUS
ARDOUGNE_NORTH
ARDOUGNE_SOUTH
AUBURNVALE
BARBARIAN_OUTPOST
BLAST_FURNACE
BUCCANEERS_HAVEN
BURGH_DE_ROTT
CAM_TORUM
CANIFIS
CASTLE_WARS
CATHERBY
CHARRED_ISLAND
CIVITAS_ILLA_FORTIS_EAST
CIVITAS_ILLA_FORTIS_WEST
COOKS_GUILD
CORSAIR_COVE
CRAFTING_GUILD
DARKFROST
DARKMEYER
DEEPFIN_MINE_EAST
DEEPFIN_MINE_WEST
DORGESH_KAAN
DRAYNOR
DUEL_ARENA
EDGEVILLE
ETCETERIA
FALADOR_EAST
FALADOR_WEST
FARMING_GUILD
FEROX_ENCLAVE
FISHING_GUILD
FOSSIL_ISLAND
GIANTS_FOUNDRY
GNOME_STRONGHOLD
GOTR
GRAND_EXCHANGE
GRAND_TREE
HALLOWED_SEPULCHER
HOSIDIUS
HOSIDIUS_KITCHEN
HOSIDIUS_SHORE
HOSIDIUS_VINERY
HOUSE_ON_THE_HILL_ISLAND
HUNTER_GUILD
JATIZSO
KELDAGRIM
KOUREND_CASTLE
LANDS_END
LLETYA
LOVAKENGJ_SOUTH
LOVAKENGJ_WEST
LUMBRIDGE
LUMBRIDGE_BASEMENT
LUNAR_ISLAND
MAGE_BANK
MAGE_TRAINING_ARENA
MINING_GUILD
MISTROCK
MIXOLOGY
MOONCLAN_ISLE
MOS_LEHARMLESS
MOTHERLODE_MINE
MOUNT_KARUULM
MOUNT_QUIDAMORTEM
MYTHS_GUILD
NARDAH
NEITIZNOT
NEMUS_RETREAT
NORTH_PRIFDDINAS
PEST_CONTROL
PISCATORIS
PORT_KHAZARD
PORT_PHASMATYS
PORT_PISCARILIUS
QUETZACALLI_GORGE
ROGUES_DEN
RUINS_OF_CAMDOZAAL
RUINS_OF_UNKAH
SEERS
SHANTAY_PASS
SHAYZIEN
SHAYZIEN_ENCAMPMENT
SHILO_VILLAGE
SOPHANEM_DUNGEON
SOUL_WARS
SOUTH_PRIFDDINAS
SULFUR_MINE
SUNBLEAK_ISLAND
TAL_TEKLAN
THE_ONYX_CREST
THE_PANDEMONIUM
TZHAAR
TZHAAR_EAST
VARROCK_EAST
VARROCK_WEST
VER_SINHAZA
WARRIORS_GUILD
WINTERTODT
WOODCUTTING_GUILD
WOODCUTTING_GUILD_DUNGEON
YANILLE
ZANARIS
```

**Methods:**
```java
static boolean blacklist(BankLocation location);
static void clearBlacklistedLocations();
Area getArea(int radius);
BankType getBankType();
Tile getCenter();
Condition getCondition();
static BankLocation getNearest();
static BankLocation getNearest(Tile tile);
static BankLocation getNearest(Tile tile, boolean includeTeleports);
static BankLocation getNearest(Entity entity);
static BankLocation getNearestEuclidean(Tile tile, boolean ignoreRequirements);
static List<BankLocation> getSortedValidLocations(Tile nearest);
static List<BankLocation> getSortedValidLocations(Entity nearest);
Tile getTile();
static List<BankLocation> getValidLocations();
static boolean isBlacklisted(BankLocation location);
boolean isMembersOnly();
static void resetCache();
String toString();
static boolean unblacklist(BankLocation location);
static BankLocation valueOf(String name);
static BankLocation [] values();
```

| Method | Description |
|--------|-------------|
| `getArea(int radius)` | Generates an Area around the center Tile based on radius |
| `getBankType()` | Gets the BankType |
| `getCenter()` | Gets the center Tile of the bank |
| `getCondition()` | Gets any requirements needed to access this bank |
| `getNearest(Tile tile)` | Gets the nearest BankLocation to the tile |
| `getNearest(Tile tile, boolean includeTeleports)` | Gets the nearest BankLocation to the given Tile |
| `getNearest(Entity entity)` | Gets the nearest BankLocation to the entity |
| `getNearestEuclidean(Tile tile, boolean ignoreRequirements)` | Gets the nearest bank location to the entity based on euclidean distance getNearest(Tile) will check against web paths to find more exact distances, so takes longer This method should be quicker call, while possibly being less precise. |
| `getTile()` | Gets tile of the object. |
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

**Inherited methods:**
- From `Locatable`: `canReach`, `canReach`, `distance`, `getGridX`, `getGridY`, `getRegionId`, `getTileReference`, `getX`, `getY`, `getZ`, `tileDistance`, `walkingDistance`

---

### BankMode

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<BankMode>org.dreambot.api.methods.container.impl.bank.BankMode → java.lang.Enum<BankMode> → org.dreambot.api.methods.container.impl.bank.BankMode → org.dreambot.api.methods.container.impl.bank.BankMode  

**Constants:**
```java
INSERT
ITEM
NOTE
SWAP
```

**Methods:**
```java
int getChildId();
String toString();
static BankMode valueOf(String name);
static BankMode [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### BankQuantitySelection

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<BankQuantitySelection>org.dreambot.api.methods.container.impl.bank.BankQuantitySelection → java.lang.Enum<BankQuantitySelection> → org.dreambot.api.methods.container.impl.bank.BankQuantitySelection → org.dreambot.api.methods.container.impl.bank.BankQuantitySelection  

**Constants:**
```java
ALL
FIVE
ONE
TEN
X
```

**Methods:**
```java
int getChildId();
static BankQuantitySelection getSelection();
String toString();
static BankQuantitySelection valueOf(String name);
static BankQuantitySelection [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### BankScroll

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<BankScroll>org.dreambot.api.methods.container.impl.bank.BankScroll → java.lang.Enum<BankScroll> → org.dreambot.api.methods.container.impl.bank.BankScroll → org.dreambot.api.methods.container.impl.bank.BankScroll  

**Constants:**
```java
CLICK
WHEEL
```

**Methods:**
```java
int getId();
static BankScroll valueOf(String name);
static BankScroll [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### BankTab

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<BankTab>org.dreambot.api.methods.container.impl.bank.BankTab → java.lang.Enum<BankTab> → org.dreambot.api.methods.container.impl.bank.BankTab → org.dreambot.api.methods.container.impl.bank.BankTab  

**Constants:**
```java
EIGHTH_TAB
FIFTH_TAB
FIRST_TAB
FOURTH_TAB
MAIN_TAB
NINTH_TAB
SECOND_TAB
SEVENTH_TAB
SIXTH_TAB
THIRD_TAB
```

**Methods:**
```java
int count();
int getChild();
int getConfig();
int getId();
int getID();
static BankTab getOpen();
WidgetChild getWidgetChild();
boolean isOpen();
boolean open();
static BankTab valueOf(String name);
static BankTab [] values();
```

| Method | Description |
|--------|-------------|
| `getChild()` | Gets the grand child WidgetChild ID of the BankTab |
| `getConfig()` | Gets the config value for the BankTab |
| `getId()` | Gets the ID of the BankTab |
| `getID()` | Deprecated. |
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### BankType

**Package:** `org.dreambot.api.methods.container.impl.bank`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<BankType>org.dreambot.api.methods.container.impl.bank.BankType → java.lang.Enum<BankType> → org.dreambot.api.methods.container.impl.bank.BankType → org.dreambot.api.methods.container.impl.bank.BankType  

**Constants:**
```java
BOOTH
BUFFALO
CHEST
EXCHANGE
NPC
TABLE
```

**Methods:**
```java
String[] getActions();
static BankType valueOf(String name);
static BankType [] values();
```

| Method | Description |
|--------|-------------|
| `getActions()` | Gets the banking actions of the BankType |
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### ContainerType

**Package:** `org.dreambot.api.methods.container.impl`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<ContainerType>org.dreambot.api.methods.container.impl.ContainerType → java.lang.Enum<ContainerType> → org.dreambot.api.methods.container.impl.ContainerType → org.dreambot.api.methods.container.impl.ContainerType  

**Constants:**
```java
BANK
DEPOSIT_BOX
EQUIPMENT
INVENTORY
LOOTING_BAG
SHOP
TRADE
```

**Methods:**
```java
static ContainerType valueOf(String name);
static ContainerType [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### DepositBox

**Package:** `org.dreambot.api.methods.depositbox`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.depositbox.DepositBox → org.dreambot.api.methods.depositbox.DepositBox  

**Methods:**
```java
static void add(int index, Item t);
static boolean add(Item t);
static List<Item> all();
static List<Item> all(Filter <Item> filter);
static int capacity();
static boolean close();
static boolean contains(int id);
static boolean contains(Integer... ids);
static boolean contains(Object o);
static boolean contains(String string);
static boolean contains(String... names);
static boolean contains(Filter <Item> filter);
static boolean containsAll(int... ids);
static boolean containsAll(String... names);
static boolean containsAll(Collection<?> collection);
static int count(int id);
static int count(String name);
static int count(Filter <Item> filter);
static boolean deposit(int id);
static boolean deposit(int id, int amount);
static boolean deposit(String name);
static boolean deposit(String name, int amount);
static boolean deposit(Filter <Item> filter);
static boolean deposit(Filter <Item> filter, int amount);
static boolean deposit(Item item);
static boolean deposit(Item item, int amount);
static boolean depositAll(int id);
static boolean depositAll(String name);
static boolean depositAll(Filter <Item> filter);
static boolean depositAll(Item item);
static boolean depositAllEquipment();
static boolean depositAllExcept(Integer... ids);
static boolean depositAllExcept(String... names);
static boolean depositAllExcept(Filter <Item> filter);
static boolean depositAllItems();
static boolean depositAllLoot();
static int emptySlotCount();
static List<Item> except(Filter <Item> filter);
static int fullSlotCount();
static Item get(int id);
static Item get(int[] ids);
static Item get(Integer... ids);
static Item get(String name);
static Item get(String... names);
static Item get(Filter <Item> filter);
static int getFirstEmptySlot();
static int getFirstFullSlot();
static int getIdForSlot(int slot);
static Item getItemInSlot(int slot);
static String getNameForSlot(int slot);
static WidgetChild getSlotWidget(int slot);
static boolean isEmpty();
static boolean isFull();
static boolean isOpen();
static boolean isSlotEmpty(int slot);
static boolean isSlotFull(int slot);
static boolean onlyContains(Integer... id);
static boolean onlyContains(String... names);
static boolean onlyContains(Filter <Item> f);
static boolean open();
static boolean openClosest();
static Item set(int index, Item element);
static int size();
static int slot(int id);
static int slot(String name);
static int slot(Filter <Item> filter);
static Rectangle slotBounds(int slot);
static boolean slotContains(int slot, int[] ids);
static boolean slotContains(int slot, Integer... ids);
static boolean slotContains(int slot, String name);
static boolean slotContains(int slot, String... names);
static boolean slotContains(int slot, Filter <Item> filter);
static boolean slotContains(int slot, Item t);
static Object[] toArray();
static int widgetChildId();
static int widgetParentId();
```

| Method | Description |
|--------|-------------|
| `all(Filter <Item> filter)` | A list of all the filtered items. |
| `close()` | Attempts to close an opened deposit box, if open |
| `contains(int id)` | Checks if deposit box contains an Item with given ID |
| `contains(Integer... ids)` | Checks if your collection contains at least one item which meets any of the specified ids. |
| `contains(String string)` | Determines if deposit box contains item with specified name. |
| `contains(String... names)` | Checks if your collection contains at least one item which meets any of the specified names. |
| `contains(Filter <Item> filter)` | Checks if your collection contains at least one item which meets the specified filter. |
| `containsAll(int... ids)` | Checks if the deposit box contains all of the items with specified IDs |
| `containsAll(String... names)` | Checks if the deposit box contains all of the items with names specified |
| `containsAll(Collection<?> collection)` | Checks if the deposit box contains all of the checks in the given Collection |
| `count(int id)` | Count of all the items that match the search. |
| `count(String name)` | Count of all the items that match the search. |
| `count(Filter <Item> filter)` | Count of all the items that match the search. |
| `emptySlotCount()` | Empty slot count. |
| `except(Filter <Item> filter)` | Get all items which do not fit the given criteria. |
| `fullSlotCount()` | Full slot count. |
| `get(int id)` | Gets the first item with the given ID |
| `get(int[] ids)` | see get(Integer...) |
| `get(Integer... ids)` | Gets the first item with the given IDs |
| `get(String name)` | Get item for specified name. |
| `get(String... names)` | Gets the first item that matches one of the given names |
| `get(Filter <Item> filter)` | Gets the first item that matches the given filter |
| `getFirstEmptySlot()` | Gets the first empty slot in the deposit box. |
| `getFirstFullSlot()` | Gets the next full slot in the deposit box |
| `getIdForSlot(int slot)` | Gets the Id of the Item in the slot. |
| `getItemInSlot(int slot)` | Gets item in specified slot. |
| `getNameForSlot(int slot)` | Gets the Name for the Item in the slot. |
| `isFull()` | Determines if container full. |
| `isOpen()` | Checks whether you have a deposit box open |
| `isSlotFull(int slot)` | Checks if the slot has an Item in it |
| `onlyContains(Integer... id)` | Checks if the deposit box only contains items with specified ID |
| `onlyContains(String... names)` | Checks if your Inventory only contains items with specified name |
| `onlyContains(Filter <Item> f)` | Checks if the deposit box only contains items that match the Filter |
| `open()` | Attempts to open the nearest loaded Bank Deposit Box |
| `openClosest()` | see open() |
| `slot(int id)` | Gets the slot for the item with specified ID. |
| `slot(String name)` | Gets the slot for the item with specified name. |
| `slot(Filter <Item> filter)` | Gets the slot for the item with specified filter. |
| `slotBounds(int slot)` | Gets the Rectangle of the slot |
| `slotContains(int slot, int[] ids)` | See slotContains(int, Integer...) |
| `slotContains(int slot, Integer... ids)` | Checks if the slot contains items that matches the given ids. |
| `slotContains(int slot, String name)` | Checks if the slot contains an item with the name containing the given name |
| `slotContains(int slot, String... names)` | Checks if the slot contains items that matches the given names. |
| `slotContains(int slot, Filter <Item> filter)` | Checks if the slot contains an item that matches the filter. |
| `slotContains(int slot, Item t)` | Checks if the slot contains the given Item |

---

### DropPattern

**Package:** `org.dreambot.api.methods.container.impl`  
**Type:** interface  

**Methods:**
```java
java.util.Comparator<Item> getComparator();
```

---

### Equipment

**Package:** `org.dreambot.api.methods.container.impl.equipment`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.impl.equipment.Equipment → org.dreambot.api.methods.container.impl.equipment.Equipment  

**Methods:**
```java
static void add(int index, Item t);
static boolean add(Item t);
static List<Item> all();
static List<Item> all(Filter <Item> filter);
static int capacity();
static boolean contains(int id);
static boolean contains(int[] ids);
static boolean contains(Object o);
static boolean contains(Filter <Item> filter);
static boolean contains(Integer... ids);
static boolean contains(String string);
static boolean contains(String... names);
static boolean containsAll(int... ids);
static boolean containsAll(Collection<?> collection);
static boolean containsAll(String... names);
static int count(int id);
static int count(Filter <Item> filter);
static int count(String name);
static int emptySlotCount();
static boolean equip(EquipmentSlot slot, int[] ids);
static boolean equip(EquipmentSlot slot, Filter <Item> filter);
static boolean equip(EquipmentSlot slot, Integer... ids);
static boolean equip(EquipmentSlot slot, String... names);
static List<Item> except(Filter <Item> filter);
static int fullSlotCount();
static Item get(int id);
static Item get(int[] ids);
static Item get(Filter <Item> filter);
static Item get(Integer... ids);
static Item get(String name);
static Item get(String... names);
static int getFirstEmptySlot();
static int getFirstFullSlot();
static int getIdForSlot(int slot);
static int getIdForSlot(EquipmentSlot slot);
static Item getItemInSlot(int slot);
static Item getItemInSlot(EquipmentSlot equipmentSlot);
static String getNameForSlot(int slot);
static String getNameForSlot(EquipmentSlot slot);
static List<Item> getRaw();
static Rectangle getSlotBounds(EquipmentSlot slot);
static EquipmentSlot getSlotForItem(Filter <Item> filter);
static WidgetChild getWidgetForSlot(EquipmentSlot slot);
static boolean interact(EquipmentSlot slot, String action);
static boolean isEmpty();
static boolean isFull();
static boolean isLoaded();
static boolean isOpen();
static boolean isSlotEmpty(int slot);
static boolean isSlotEmpty(EquipmentSlot slot);
static boolean isSlotFull(int slot);
static boolean isSlotFull(EquipmentSlot slot);
static boolean onlyContains(int[] ids);
static boolean onlyContains(Filter <Item> filter);
static boolean onlyContains(Integer... ids);
static boolean onlyContains(String... names);
static boolean open();
static void reset();
static void reset(boolean full);
static Item set(int index, Item element);
static int size();
static int slot(int id);
static int slot(String name);
static int slot(Filter <Item> filter);
static boolean slotContains(int slot, int[] ids);
static boolean slotContains(int slot, Filter <Item> filter);
static boolean slotContains(int slot, Item item);
static boolean slotContains(int slot, Integer... ids);
static boolean slotContains(int slot, String name);
static boolean slotContains(int slot, String... names);
static boolean slotContains(EquipmentSlot slot, int[] ids);
static boolean slotContains(EquipmentSlot slot, Filter <Item> filter);
static boolean slotContains(EquipmentSlot slot, Item item);
static boolean slotContains(EquipmentSlot slot, Integer... ids);
static boolean slotContains(EquipmentSlot slot, String name);
static boolean slotContains(EquipmentSlot slot, String... names);
static Object [] toArray();
static boolean unequip(EquipmentSlot slot);
static boolean unequip(Filter <Item> filter);
static void update();
static int widgetChildId();
static int widgetParentId();
```

| Method | Description |
|--------|-------------|
| `all(Filter <Item> filter)` | A list of all the filtered items. |
| `capacity()` | Gets the capacity for the equipment interface |
| `contains(int id)` | Contains boolean. |
| `contains(Filter <Item> filter)` | Checks if your equipment contains one item which meets the specified filter. |
| `contains(Integer... ids)` | Checks if your equipment contains one item which meets one of the specified ids. |
| `contains(String string)` | Determines if collection contains item with specified name. |
| `contains(String... names)` | Checks if your equipment contains one item which meets one of the specified names. |
| `containsAll(int... ids)` | Checks if your equipment contains all of the items with specified IDs |
| `containsAll(Collection<?> collection)` | Checks if your equipment contains all of the items in the collections |
| `containsAll(String... names)` | Checks if your equipment contains all of the items with names specified |
| `count(int id)` | Count of all the items that match the search. |
| `count(Filter <Item> filter)` | Count of all the items that match the search. |
| `count(String name)` | Count of all the items that match the search. |
| `emptySlotCount()` | Gets the number of slots that do not contain items |
| `equip(EquipmentSlot slot, int[] ids)` | see equip(EquipmentSlot, Integer...) |
| `equip(EquipmentSlot slot, Filter <Item> filter)` | Equips the first item that matches the given filter in the slot provided |
| `equip(EquipmentSlot slot, Integer... ids)` | Equips the first item that matches one of the given IDs in the slot provided |
| `equip(EquipmentSlot slot, String... names)` | Equips the first item that matches one of the given names in the slot provided |
| `except(Filter <Item> filter)` | Get all items which do not fit the given criteria. |
| `fullSlotCount()` | Gets the number of slots that contains items |
| `get(String name)` | Get item for specified name. |
| `getFirstEmptySlot()` | Gets the first empty slot in your equipment. |
| `getFirstFullSlot()` | Gets the next full slot in your equipment |
| `getIdForSlot(int slot)` | Gets the Id of the Item in the slot. |
| `getIdForSlot(EquipmentSlot slot)` | Gets the ID of the Item in the EquipmentSlot |
| `getItemInSlot(int slot)` | Gets item in specified slot. |
| `getItemInSlot(EquipmentSlot equipmentSlot)` | Gets item in specified slot. |
| `getNameForSlot(int slot)` | Gets the Name for the Item in the slot. |
| `getNameForSlot(EquipmentSlot slot)` | Gets the Name for the Item in the EquipmentSlot . |
| `getSlotBounds(EquipmentSlot slot)` | Gets the rectangular bounds of the WidgetChild for the given EquipmentSlot |
| `getSlotForItem(Filter <Item> filter)` | Gets the EquipmentSlot for a given item filter |
| `getWidgetForSlot(EquipmentSlot slot)` | Gets the WidgetChild for the EquipmentSlot given |
| `interact(EquipmentSlot slot, String action)` | Interacts with the slot specified with the action specified Opens the Tab.EQUIPMENT tab if needed |
| `isFull()` | Determines if equipment full. |
| `isLoaded()` | Checks if the internal equipment tables have been loaded Used primarily for ItemEventManagers |
| `isSlotEmpty(int slot)` | Checks if the given slot is empty Uses the values from EquipmentSlot.getSlot() |
| `isSlotEmpty(EquipmentSlot slot)` | Checks if the given EquipmentSlot is empty |
| `isSlotFull(int slot)` | Checks if the slot has an Item in it slot is based on EquipmentSlot.getSlot() |
| `isSlotFull(EquipmentSlot slot)` | Checks if the given EquipmentSlot is full |
| `onlyContains(int[] ids)` | See onlyContains(Integer...) |
| `onlyContains(Filter <Item> filter)` | Checks if your Equipment only contains items that match the Filter |
| `onlyContains(Integer... ids)` | Checks if your Equipment only contains items with specified IDs |
| `onlyContains(String... names)` | Checks if your equipment only contains items with specified names |
| `open()` | Opens the equipment tab |
| `slot(int id)` | Gets the slot for the item with specified ID. |
| `slot(String name)` | Gets the slot for the item with specified name. |
| `slot(Filter <Item> filter)` | Gets the slot for the item with specified name. |
| `slotContains(int slot, int[] ids)` | see slotContains(int, Integer...) |
| `slotContains(int slot, Filter <Item> filter)` | Checks if the slot contains items that matches the filter. |
| `slotContains(int slot, Item item)` | Checks if the slot contains the given Item slot based on EquipmentSlot.getSlot() |
| `slotContains(int slot, Integer... ids)` | Checks if the slot contains an item that matches any the given ids. |
| `slotContains(int slot, String name)` | Checks if the slot contains an Item that matches the given name. |
| `slotContains(int slot, String... names)` | Checks if the slot contains any items that match the given names. |
| `slotContains(EquipmentSlot slot, int[] ids)` | see slotContains(int, Integer...) |
| `slotContains(EquipmentSlot slot, Filter <Item> filter)` | Checks if the EquipmentSlot contains items that matches the given filter. |
| `slotContains(EquipmentSlot slot, Item item)` | Checks if the EquipmentSlot contains the given Item |
| `slotContains(EquipmentSlot slot, Integer... ids)` | Checks if the EquipmentSlot contains an item that matches any the given ids. |
| `slotContains(EquipmentSlot slot, String name)` | Checks if the EquipmentSlot contains an item that matches the given name. |
| `slotContains(EquipmentSlot slot, String... names)` | Checks if the EquipmentSlot contains any items that match the given names. |
| `unequip(EquipmentSlot slot)` | Unequips the equipment from the equipment slot |
| `unequip(Filter <Item> filter)` | Unequips an item that matches the given Filter |
| `widgetChildId()` | Gets the child ID of the WidgetChild for the equipment interface |
| `widgetParentId()` | Gets the ID of the parent Widget for the Equipment Interface |

---

### EquipmentSlot

**Package:** `org.dreambot.api.methods.container.impl.equipment`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<EquipmentSlot>org.dreambot.api.methods.container.impl.equipment.EquipmentSlot → java.lang.Enum<EquipmentSlot> → org.dreambot.api.methods.container.impl.equipment.EquipmentSlot → org.dreambot.api.methods.container.impl.equipment.EquipmentSlot  

**Constants:**
```java
AMULET
ARROWS
CAPE
CHEST
FEET
HANDS
HAT
LEGS
RING
SHIELD
WEAPON
```

**Methods:**
```java
static EquipmentSlot forOriginalSlotId(int id);
static EquipmentSlot forSlotId(int id);
String[] getActions();
int getChildWidget();
Rectangle getPosition();
int getRealSlot();
int getSlot();
boolean hasAction(String action);
static void resetBaseChildId();
static EquipmentSlot valueOf(String name);
static EquipmentSlot [] values();
```

| Method | Description |
|--------|-------------|
| `forSlotId(int id)` | Gets the EquipmentSlot based on getRealSlot() |
| `getActions()` | Gets the actions of the Equipment currently in this slot |
| `getChildWidget()` | Gets the WidgetChild ID of the Equipment |
| `getRealSlot()` | Gets the real slot of the equipment, used for the item tables |
| `getSlot()` | Gets the slot index of the equipment |
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### Inventory

**Package:** `org.dreambot.api.methods.container.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.impl.Inventory → org.dreambot.api.methods.container.impl.Inventory  

**Fields:**
```java
static int SLOT_HEIGHT
static int SLOT_WIDTH
```

**Methods:**
```java
static void add(int index, Item t);
static boolean add(Item t);
static List<Item> all();
static List<Item> all(Filter <Item> filter);
static int capacity();
static boolean combine(int primaryItemId, int secondaryItemId);
static boolean combine(String primaryItemName, String secondaryItemName);
static boolean combine(Item primary, Item secondary);
static boolean contains(int id);
static boolean contains(int[] ids);
static boolean contains(String string);
static boolean contains(String... names);
static boolean contains(Filter <Item> filter);
static boolean contains(Object o);
static boolean contains(Integer... ids);
static boolean containsAll(int... ids);
static boolean containsAll(String... names);
static boolean containsAll(Collection<?> collection);
static int count(int id);
static int count(String name);
static int count(Filter <Item> filter);
static boolean deselect();
static boolean drag(int itemId, int toSlot);
static boolean drag(String itemName, int toSlot);
static boolean drag(Filter <Item> itemFilter, int toSlot);
static boolean drag(Item item, int toSlot);
static boolean drop(int id);
static boolean drop(String name);
static boolean drop(Filter <Item> filter);
static boolean dropAll();
static boolean dropAll(int id);
static boolean dropAll(int[] ids);
static boolean dropAll(String name);
static boolean dropAll(String... names);
static boolean dropAll(Filter <Item> filter);
static boolean dropAll(Integer... ids);
static boolean dropAllExcept(int[] ids);
static boolean dropAllExcept(String... names);
static boolean dropAllExcept(Filter <Item> filter);
static boolean dropAllExcept(Integer... ids);
static int emptySlotCount();
static List<Item> except(Filter <Item> filter);
static int fullSlotCount();
static Item get(int id);
static Item get(int[] ids);
static Item get(String name);
static Item get(String... names);
static Item get(Integer... ids);
static Item get(Filter <Item> filter);
static String[] getActionsForSlot(int slot);
static DropPattern getDropPattern();
static int getEmptySlots();
static int getFirstEmptySlot();
static int getFirstFullSlot();
static int getIdForSlot(int slot);
static WidgetChild getInventoryWidget();
static Item getItemInSlot(int index);
static String getNameForSlot(int slot);
static Item getRandom(int... itemIds);
static Item getRandom(String... itemNames);
static Item getRandom(Filter <Item> itemFilter);
static int getSelectedItemId();
static int getSelectedItemIndex();
static String getSelectedItemName();
static WidgetChild getWidgetForSlot(int slot);
static WidgetChild getWidgetForSlot(int slot, String action);
static boolean interact(int id);
static boolean interact(int id, String action);
static boolean interact(String name);
static boolean interact(String name, String action);
static boolean interact(Filter <Item> filter);
static boolean interact(Filter <Item> filter, String action);
static boolean interact(Item item);
static boolean interact(Item item, String action);
static boolean isEmpty();
static boolean isForceNoShift();
static boolean isFull();
static boolean isItemSelected();
static boolean isLoaded();
static boolean isOpen();
static boolean isSlotEmpty(int slot);
static boolean isSlotFull(int slot);
static Rectangle itemBounds(Item item);
static boolean onlyContains(int[] id);
static boolean onlyContains(String... names);
static boolean onlyContains(Filter <Item> f);
static boolean onlyContains(Integer... id);
static boolean open();
static void reset();
static void reset(boolean full);
static Item set(int index, Item element);
static void setDropPattern(DropPattern dropPattern);
static void setForceNoShift(boolean forceNoShift);
static void setSelectedWidgetItemId(int id);
static boolean shouldShift();
static int size();
static int slot(int id);
static int slot(String name);
static int slot(Filter <Item> filter);
static Rectangle slotBounds(int slot);
static boolean slotContains(int slot, int[] ids);
static boolean slotContains(int slot, Integer... ids);
static boolean slotContains(int slot, String... names);
static boolean slotContains(int slot, Filter <Item> filter);
static boolean slotContains(int slot, Item t);
static boolean slotInteract(int slot);
static boolean slotInteract(int slot, String action);
static boolean slotNameContains(int slot, String sub);
static boolean swap(int fromSlot, int toSlot);
static boolean swap(Item firstItem, Item secondItem);
static Item [] toArray();
static void update();
static boolean use(int id);
static boolean use(String name);
static boolean use(Item item);
static int widgetChildId();
static int widgetParentId();
```

| Method | Description |
|--------|-------------|
| `add(int index, Item t)` | Adds an item manually to the inventory collection at the given index Not recommended unless you know what you're doing |
| `add(Item t)` | Adds an item manually to the Inventory collection Not recommended unless you know what you're doing. |
| `all()` | Gets the items in your inventory |
| `all(Filter <Item> filter)` | A list of all the items. |
| `capacity()` | Returns the capacity of your inventory (28) |
| `contains(int id)` | Contains boolean. |
| `contains(String string)` | Determines if collection contains item with specified name. |
| `contains(String... names)` | Checks if your collection contains one item which meets one of the specified names. |
| `contains(Filter <Item> filter)` | Checks if your collection contains one item which meets the specified filter. |
| `contains(Integer... ids)` | Checks if your collection contains one item which meets one of the specified ids. |
| `containsAll(int... ids)` | Checks if your Inventory contains all of the items with specified IDs |
| `containsAll(String... names)` | Checks if your Inventory contains all of the items with names specified |
| `containsAll(Collection<?> collection)` | Checks if your collection contains all of the items in the collections |
| `count(int id)` | Count of all the items that match the search. |
| `count(String name)` | Count of all the items that match the search. |
| `count(Filter <Item> filter)` | Count of all the items that match the search. |
| `deselect()` | Deselects any item if it is selected |
| `drag(int itemId, int toSlot)` | This will move the first matching item in the inventory to the provided slot. |
| `drag(String itemName, int toSlot)` | This will move the first matching item in the inventory to the provided slot. |
| `drag(Filter <Item> itemFilter, int toSlot)` | This will move the first matching item in the inventory to the provided slot. |
| `drag(Item item, int toSlot)` | This will move the first matching item in the inventory to the provided slot. |
| `drop(int id)` | Drops the item with specified ID from inventory |
| `drop(String name)` | Drops the item with specified name from Inventory |
| `drop(Filter <Item> filter)` | Drops the item matching specified Filter from Inventory |
| `dropAll()` | Drops all items in your inventory. |
| `dropAll(int id)` | Drops all items in your inventory matching specified ID |
| `dropAll(int[] ids)` | see dropAll(Integer...) |
| `dropAll(String name)` | Drops all items in your inventory matching specified name |
| `dropAll(String... names)` | Drops all items in your inventory matching specified names. |
| `dropAll(Filter <Item> filter)` | Drops all items in your inventory matching the specified filter |
| `dropAll(Integer... ids)` | Drops all items in your inventory matching specified ids |
| `dropAllExcept(int[] ids)` | see dropAllExcept(Integer...) |
| `dropAllExcept(String... names)` | Drops all items that don't match the given names |
| `dropAllExcept(Filter <Item> filter)` | Drops all items that are not filtered. |
| `dropAllExcept(Integer... ids)` | Drops all items that don't match the given ids |
| `emptySlotCount()` | Empty slot count. |
| `except(Filter <Item> filter)` | Get all items which do not fit the given criteria. |
| `fullSlotCount()` | Full slot count. |
| `get(int id)` | Gets the first item that matches the given item ID |
| `get(String name)` | Get item for specified name. |
| `get(Integer... ids)` | Gets the first item that matches one of the given IDs |
| `getDropPattern()` | Gets the dropping pattern for dropAll(Filter) Defaults to StandardDropPattern.LEFT_TO_RIGHT_THEN_DOWN |
| `getEmptySlots()` | Gets the count of empty slots in your inventory |
| `getFirstEmptySlot()` | Gets the first empty slot in your inventory. |
| `getFirstFullSlot()` | Gets the next full slot in your inventory |
| `getIdForSlot(int slot)` | Gets the Id of the Item in the slot. |
| `getInventoryWidget()` | Gets the basic inventory widget Basic meaning bank not open, ge not open, shop not open, etc |
| `getItemInSlot(int index)` | Gets item in specified slot. |
| `getNameForSlot(int slot)` | Gets the Name for the Item in the slot. |
| `getRandom(int... itemIds)` | Gets a random item that matches the item id(s) in your inventory |
| `getRandom(String... itemNames)` | Gets a random item that matches the item name(s) in your inventory |
| `getRandom(Filter <Item> itemFilter)` | Gets a random item that matches the item filter in your inventory |
| `getSelectedItemId()` | Gets the ID of the currently selected item. |
| `getSelectedItemIndex()` | Gets the index of the currently selected item. |
| `getSelectedItemName()` | Gets the currently selected item name |
| `interact(int id)` | Interact with an Item with specified ID in inventory using the first available action |
| `interact(int id, String action)` | Interact with an Item with specified ID in inventory |
| `interact(String name)` | Interact with the Item with specified name in Inventory using the first available action |
| `interact(String name, String action)` | Interact with the Item with specified name in Inventory |
| `interact(Filter <Item> filter)` | Interact with an Item with specified ID in inventory using the first available action |
| `interact(Filter <Item> filter, String action)` | Interact with an Item with specified ID in inventory |
| `interact(Item item)` | Interacts with a given Item with a specified action using the first available action If Item has an invalid slot value, it will find slot based on item id |
| `interact(Item item, String action)` | Interacts with a given Item with a specified action If Item has an invalid slot value, it will find slot based on item id |
| `isForceNoShift()` | Forces the inventory to ignore whether shift interactions are enabled or not |
| `isFull()` | Determines if container full. |
| `isItemSelected()` | Checks whether an item is currently selected in your inventory |
| `isLoaded()` | Checks if the Inventory Item Table has been successfully loaded within the client through our all() call |
| `isSlotEmpty(int slot)` | Checks if the given slot has an item in it or not |
| `isSlotFull(int slot)` | Checks if the slot has an Item in it |
| `itemBounds(Item item)` | Gets the slot bounds of an item. |
| `onlyContains(int[] id)` | see onlyContains(Integer...) |
| `onlyContains(String... names)` | Checks if your Inventory only contains items with specified name |
| `onlyContains(Filter <Item> f)` | Checks if your Inventory only contains items that match the Filter |
| `onlyContains(Integer... id)` | Checks if your Inventory only contains items with specified ID |
| `setDropPattern(DropPattern dropPattern)` | Sets the dropping pattern for dropAll(Filter) |
| `setForceNoShift(boolean forceNoShift)` | Sets inventory to force no shift during dropping |
| `shouldShift()` | Checks whether shift needs to be pressed for shift interactions Only implemented for dropping purposes |
| `slot(int id)` | Gets the slot for the item with specified ID. |
| `slot(String name)` | Gets the slot for the item with specified name. |
| `slot(Filter <Item> filter)` | Gets the slot for the item with specified name. |
| `slotBounds(int slot)` | Gets the bounds of a slot |
| `slotContains(int slot, int[] ids)` | see slotContains(int, Integer...) |
| `slotContains(int slot, Integer... ids)` | Checks if the slot contains items that matches the given ids. |
| `slotContains(int slot, String... names)` | Checks if the slot contains items that matches the given names. |
| `slotContains(int slot, Filter <Item> filter)` | Checks if the slot contains items that matches the filter. |
| `slotContains(int slot, Item t)` | Checks if the slot contains items that matches the given ids. |
| `slotInteract(int slot)` | Interacts with specified slot in inventory using the first available action |
| `slotInteract(int slot, String action)` | Interacts with specified slot in inventory |
| `slotNameContains(int slot, String sub)` | Checks if the slot contains items that matches the given ids. |
| `swap(int fromSlot, int toSlot)` | Swaps the contents of two inventory slots, either of which can be empty to simply move an item from one to the other |
| `swap(Item firstItem, Item secondItem)` | Swaps the first of each item given if found, if you want to swap specific slots use swap(int, int) instead |
| `toArray()` | Converts all() to an Item array |
| `widgetChildId()` | Gets the basic inventory widget child ID Basic meaning bank not open, ge not open, shop not open, etc |
| `widgetParentId()` | Gets the basic inventory widget parent ID Basic meaning bank not open, GE not open, shop not open, etc |

---

### ItemContainer

**Package:** `org.dreambot.api.methods.container.general`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.general.ItemContainer → org.dreambot.api.methods.container.general.ItemContainer  

**Methods:**
```java
int getContainerId();
List<Item> getItems();
```

| Method | Description |
|--------|-------------|
| `getItems()` | Gets the items in this item container Does not add empty slots to the list Does not set item destinations |

---

### ItemContainerId

**Package:** `org.dreambot.api.methods.container.general`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<ItemContainerId>org.dreambot.api.methods.container.general.ItemContainerId → java.lang.Enum<ItemContainerId> → org.dreambot.api.methods.container.general.ItemContainerId → org.dreambot.api.methods.container.general.ItemContainerId  

**Constants:**
```java
BANK
BARROWS_CHEST
CHAMBERS_OF_XERIC_CHEST
EQUIPMENT
GE_SLOT_1
GE_SLOT_2
GE_SLOT_3
GE_SLOT_4
GE_SLOT_5
GE_SLOT_6
GE_SLOT_7
GE_SLOT_8
INVENTORY
LOOTING_BAG
THEATRE_OF_BLOOD_CHEST
TOA_REWARD_CHEST
TRADE
TRADE_OTHER
WILDERNESS_LOOT_CHEST
ZULRAH_DEATH_COLLECT
```

**Methods:**
```java
int getId();
static ItemContainerId valueOf(String name);
static ItemContainerId [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### ItemContainers

**Package:** `org.dreambot.api.methods.container.general`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.general.ItemContainers → org.dreambot.api.methods.container.general.ItemContainers  

**Methods:**
```java
static ItemContainer getContainer(int id);
static ItemContainer getRlContainer(int id);
```

| Method | Description |
|--------|-------------|
| `getContainer(int id)` | Gets an item container based on the item container id Known ids can be found in ItemContainerId |

---

### Shop

**Package:** `org.dreambot.api.methods.container.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.impl.Shop → org.dreambot.api.methods.container.impl.Shop  

**Fields:**
```java
static String ACTION_TRADE
static String BUY_FIFTY
static String BUY_FIVE
static String BUY_ONE
static String BUY_TEN
static int ID_NULL_SHOP_ITEM
static String SELL_FIFTY
static String SELL_FIVE
static String SELL_ONE
static String SELL_TEN
```

**Methods:**
```java
static void add(int index, Item t);
static boolean add(Item t);
static List<Item> all();
static List<Item> all(Filter <Item> filter);
static int capacity();
static boolean close();
static boolean contains(int id);
static boolean contains(int[] ids);
static boolean contains(Integer... ids);
static boolean contains(Object o);
static boolean contains(String string);
static boolean contains(String... names);
static boolean contains(Filter <Item> filter);
static boolean containsAll(int[] ids);
static boolean containsAll(Integer... ids);
static boolean containsAll(String... names);
static boolean containsAll(Collection<?> collection);
static int count(int id);
static int count(int[] ids);
static int count(Integer... ids);
static int count(String name);
static int count(String... names);
static int count(Filter <Item> filter);
static int emptySlotCount();
static List<Item> except(Filter <Item> filter);
static int fullSlotCount();
static Item get(int id);
static Item get(int[] ids);
static Item get(Integer... ids);
static Item get(String name);
static Item get(String... names);
static Item get(Filter <Item> filter);
static int getFirstEmptySlot();
static int getFirstFullSlot();
static int getIdForSlot(int slot);
static Item getItemInSlot(int index);
static String getNameForSlot(int slot);
static Widget getParent();
static Rectangle getSlotBounds(int slot);
static Rectangle getSlotBounds(Item item);
static String getTitle();
static boolean interact(Filter <Item> filter, String action);
static boolean isEmpty();
static boolean isFull();
static boolean isOpen();
static boolean isSlotEmpty(int slot);
static boolean isSlotFull(int slot);
static boolean onlyContains(int[] id);
static boolean onlyContains(Integer... id);
static boolean onlyContains(String... names);
static boolean onlyContains(Filter <Item> f);
static boolean open();
static boolean open(int shopId);
static boolean open(String shopName);
static boolean purchase(int id, int amount);
static boolean purchase(String name, int amount);
static boolean purchase(Filter <Item> filter, int amount);
static boolean purchase(Item item, int amount);
static boolean purchaseFifty(int id);
static boolean purchaseFifty(String name);
static boolean purchaseFifty(Filter <Item> filter);
static boolean purchaseFifty(Item item);
static boolean purchaseFive(int id);
static boolean purchaseFive(String name);
static boolean purchaseFive(Filter <Item> filter);
static boolean purchaseFive(Item item);
static boolean purchaseOne(int id);
static boolean purchaseOne(String name);
static boolean purchaseOne(Filter <Item> filter);
static boolean purchaseOne(Item item);
static boolean purchaseTen(int id);
static boolean purchaseTen(String name);
static boolean purchaseTen(Filter <Item> filter);
static boolean purchaseTen(Item item);
static boolean sell(int id, int amount);
static boolean sell(String name, int amount);
static boolean sell(Filter <Item> filter, int amount);
static boolean sell(Item item, int amount);
static boolean sellFifty(int id);
static boolean sellFifty(String name);
static boolean sellFifty(Filter <Item> filter);
static boolean sellFifty(Item item);
static boolean sellFive(int id);
static boolean sellFive(String name);
static boolean sellFive(Filter <Item> filter);
static boolean sellFive(Item item);
static boolean sellOne(int id);
static boolean sellOne(String name);
static boolean sellOne(Filter <Item> filter);
static boolean sellOne(Item item);
static boolean sellTen(int id);
static boolean sellTen(String name);
static boolean sellTen(Filter <Item> filter);
static boolean sellTen(Item item);
static Item set(int index, Item element);
static int size();
static int slot(int id);
static int slot(String name);
static int slot(Filter <Item> filter);
static boolean slotContains(int slot, int[] ids);
static boolean slotContains(int slot, Integer... ids);
static boolean slotContains(int slot, String... names);
static boolean slotContains(int slot, Filter <Item> filter);
static boolean slotContains(int slot, Item t);
static boolean slotNameContains(int slot, String sub);
static Object[] toArray();
```

| Method | Description |
|--------|-------------|
| `all(Filter <Item> filter)` | A list of all the filtered items. |
| `close()` | Closes the shop interface. |
| `contains(int id)` | Checks if the Shop contains an Item with the given id |
| `contains(Integer... ids)` | Checks if the Shop contains at least one item which meets one of the specified ids. |
| `contains(Object o)` | Checks if the Shop contains an Item matching the given Object Object could be Integer, String, Filter, or a given Item Logs an error if object type is not supported. |
| `contains(String string)` | Determines if the Shop contains item with specified name. |
| `contains(String... names)` | Checks if the Shop contains at least one item which meets one of the specified names. |
| `contains(Filter <Item> filter)` | Checks if the SHop contains at least one item which meets the specified filter. |
| `containsAll(int[] ids)` | Checks if the Shop contains all the items with specified IDs |
| `containsAll(Integer... ids)` | See containsAll(int[]) |
| `containsAll(String... names)` | Checks if the Shop contains all the items with names specified |
| `containsAll(Collection<?> collection)` | Checks if the Shop contains all the items that match everything in the given collection Uses contains(Object) |
| `count(int id)` | Count of the item that matches the given id. |
| `count(int[] ids)` | The total count of all the items that match the given ids |
| `count(Integer... ids)` | See count(int[]) |
| `count(String name)` | Count of the item that matches the given name |
| `count(String... names)` | Count of all the items that match the given names |
| `count(Filter <Item> filter)` | Count of all the items that match the filter. |
| `emptySlotCount()` | Empty slot count. |
| `except(Filter <Item> filter)` | Get all items which do not fit the given criteria. |
| `fullSlotCount()` | Full slot count. |
| `get(int id)` | Gets the first Item matching the given ID |
| `get(int[] ids)` | See get(Integer...) |
| `get(Integer... ids)` | Gets the first Item matching the given IDs |
| `get(String name)` | Get item for specified name. |
| `get(String... names)` | Get the first item that matches one of the given names |
| `get(Filter <Item> filter)` | Gets the first item that matches the given filter |
| `getFirstEmptySlot()` | Gets the first empty slot in the Shop. |
| `getFirstFullSlot()` | Gets the next full slot in the Shop |
| `getIdForSlot(int slot)` | Gets the Id of the Item in the slot. |
| `getItemInSlot(int index)` | Gets item in the specified slot. |
| `getNameForSlot(int slot)` | Gets the Name for the Item in the slot. |
| `getParent()` | Gets parent widget for current shop. |
| `getSlotBounds(int slot)` | Gets a Rectangle of the Item's slot |
| `getSlotBounds(Item item)` | Gets a Rectangle of the Item's slot |
| `getTitle()` | Gets the title of the current shop widget. |
| `interact(Filter <Item> filter, String action)` | Interacts with an Item in the shop |
| `isFull()` | Determines if Shop full. |
| `isOpen()` | Determines if a shop widget is open. |
| `isSlotFull(int slot)` | Checks if the slot has an Item in it |
| `onlyContains(Integer... id)` | Checks if the shop only contains items with specified ID |
| `onlyContains(String... names)` | Checks if the shop only contains items with specified name |
| `onlyContains(Filter <Item> f)` | Checks if the shop only contains items that match the Filter |
| `purchase(int id, int amount)` | Buys an item from the store with an adjustable amount. |
| `purchase(String name, int amount)` | Buys an item from the shop with an adjustable amount. |
| `purchase(Filter <Item> filter, int amount)` | Buys an item from the shop with an adjustable amount. |
| `purchase(Item item, int amount)` | Buys an item from the shop with an adjustable amount. |
| `purchaseFifty(int id)` | Purchases fifty of the Item |
| `purchaseFifty(String name)` | Purchases fifty of the Item |
| `purchaseFifty(Filter <Item> filter)` | Purchases fifty of the Item |
| `purchaseFifty(Item item)` | Purchases fifty of the Item |
| `purchaseFive(int id)` | Purchases five of the Item |
| `purchaseFive(String name)` | Purchases five of the Item |
| `purchaseFive(Filter <Item> filter)` | Purchases five of the Item |
| `purchaseFive(Item item)` | Purchases five of the Item |
| `purchaseOne(int id)` | Purchases one of the Item |
| `purchaseOne(String name)` | Purchases one of the Item |
| `purchaseOne(Filter <Item> filter)` | Purchases one of the Item |
| `purchaseOne(Item item)` | Purchases one of the Item |
| `purchaseTen(int id)` | Purchases ten of the Item |
| `purchaseTen(String name)` | Purchases ten of the Item |
| `purchaseTen(Filter <Item> filter)` | Purchases ten of the Item |
| `purchaseTen(Item item)` | Purchases ten of the Item |
| `sell(int id, int amount)` | Sells an item from your inventory with an adjustable amount. |
| `sell(String name, int amount)` | Sells an item from your inventory with an adjustable amount. |
| `sell(Filter <Item> filter, int amount)` | Sells an item from your inventory with an adjustable amount. |
| `sell(Item item, int amount)` | Sells an item from your inventory with an adjustable amount. |
| `sellFifty(int id)` | Sells fifty of the Item |
| `sellFifty(String name)` | Sells fifty of the Item |
| `sellFifty(Filter <Item> filter)` | Sells fifty of the Item |
| `sellFifty(Item item)` | Sells fifty of the item. |
| `sellFive(int id)` | Sells five of the Item |
| `sellFive(String name)` | Sells five of the Item |
| `sellFive(Filter <Item> filter)` | Sells five of the Item |
| `sellFive(Item item)` | Sells five of the item. |
| `sellOne(int id)` | Sells one of the Item |
| `sellOne(String name)` | Sells one of the Item |
| `sellOne(Filter <Item> filter)` | Sells one of the Item |
| `sellOne(Item item)` | Sells one of the Item |
| `sellTen(int id)` | Sells ten of the Item |
| `sellTen(String name)` | Sells ten of the Item |
| `sellTen(Filter <Item> filter)` | Sells ten of the Item |
| `sellTen(Item item)` | Sells ten of the item. |
| `slot(int id)` | Gets the slot for the item with the specified ID. |
| `slot(String name)` | Gets the slot for the item with the specified name. |
| `slot(Filter <Item> filter)` | Gets the slot for the item that matches the given Filter |
| `slotContains(int slot, int[] ids)` | See slotContains(int, Integer...) |
| `slotContains(int slot, Integer... ids)` | Checks if the slot contains an item that matches one of the given ids |
| `slotContains(int slot, String... names)` | Checks if the slot contains an item that matches the given names. |
| `slotContains(int slot, Filter <Item> filter)` | Checks if the slot contains an item that matches the filter. |
| `slotContains(int slot, Item t)` | Checks if the slot contains the given Item |
| `slotNameContains(int slot, String sub)` | Checks if the slot contains an item that matches the given name |

---

### StandardDropPattern

**Package:** `org.dreambot.api.methods.container.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.container.impl.StandardDropPattern → org.dreambot.api.methods.container.impl.StandardDropPattern  

**Fields:**
```java
static DropPattern LEFT_TO_RIGHT_THEN_DOWN
static DropPattern TOP_TO_BOTTOM_THEN_RIGHT
```

**Methods:**
```java
java.util.Comparator<Item> getComparator();
```

---

# DreamBot API — grandexchange

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### GrandExchange

**Package:** `org.dreambot.api.methods.grandexchange`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.grandexchange.GrandExchange → org.dreambot.api.methods.grandexchange.GrandExchange  

**Fields:**
```java
static int ALL
static int GUIDE_PRICE_AMOUNT
```

**Methods:**
```java
static boolean addBuyItem(String itemName);
static boolean addBuyItem(Item item);
static boolean addSellItem(String itemName);
static boolean addSellItem(Item item);
static boolean buyItem(int itemId, int amount, int price);
static boolean buyItem(String itemName, int amount, int price);
static boolean cancelAll();
static boolean cancelOffer(int slot);
static boolean close();
static boolean collect();
static boolean collectToBank();
static boolean confirm();
static boolean contains(int[] ids);
static boolean contains(Integer... ids);
static boolean contains(String... names);
static boolean contains(Filter <Item> filter);
static WidgetChild getCloseButton();
static int getCurrentAmount();
static Item getCurrentChosenItem();
static int getCurrentChosenItemId();
static int getCurrentChosenItemID();
static int getCurrentPrice();
static int getCurrentQuantity();
static WidgetChild getDecreasePriceFivePercentButton();
static WidgetChild getDecreasePriceXPercentButton();
static WidgetChild getEnterAllButton();
static WidgetChild getEnterPriceButton();
static WidgetChild getEnterQuantityButton();
static int getFirstOpenSlot();
static WidgetChild getGuidePriceButton();
static WidgetChild getIncreasePriceFivePercentButton();
static WidgetChild getIncreasePriceXPercentButton();
static GrandExchangeItem getItem(int[] ids);
static GrandExchangeItem getItem(Integer... ids);
static GrandExchangeItem getItem(String... names);
static GrandExchangeItem getItem(Filter <Item> filter);
static WidgetChild getItemChildInSearch(String itemName);
static WidgetChild getItemChildInSearch(String name, int id);
static GrandExchangeItem [] getItems();
static Item getLastChosenItem();
static int getLastChosenItemId();
static int getLastChosenItemID();
static WidgetChild getOfferFirstItemWidget();
static WidgetChild getOfferSecondItemWidget();
static String getOfferText();
static int getOpenSlot();
static int getOpenSlots();
static String getSearchedItem();
static int getUsedSlots();
static boolean goBack();
static boolean isBuyOpen();
static boolean isGeneralOpen();
static boolean isOpen();
static boolean isReadyToCollect();
static boolean isReadyToCollect(int slot);
static boolean isSearchOpen();
static boolean isSellOpen();
static boolean isSlotEnabled(int slot);
static boolean itemVisible(WidgetChild item);
static boolean open();
static boolean open(String action);
static boolean openBuyScreen(int slot);
static boolean openSellScreen(int slot);
static boolean openSlotInterface(int slot);
static boolean readyToEnterAmount();
static boolean readyToEnterPrice();
static boolean scrollToItem(WidgetChild item);
static boolean searchItem(String itemName);
static boolean sellItem(int itemId, int amount, int price);
static boolean sellItem(String itemName, int amount, int price);
static boolean setPrice(int amount);
static boolean setQuantity(int amount);
static boolean slotContainsItem(int slot);
```

| Method | Description |
|--------|-------------|
| `addBuyItem(String itemName)` | Adds an item to the buy offer Now checks if there's already an item selected, if it's the wrong item it will attempt to click the back button instead. |
| `addBuyItem(Item item)` | Adds an item to the buy offer Now checks if there's already an item selected, if it's the wrong item it will attempt to click the back button instead. |
| `addSellItem(String itemName)` | Adds an item to the sell offer |
| `addSellItem(Item item)` | Adds an item to the sell offer |
| `buyItem(int itemId, int amount, int price)` | Puts in a buy order in the Grand Exchange |
| `buyItem(String itemName, int amount, int price)` | Puts in a buy order in the Grand Exchange |
| `cancelAll()` | Cancels all available offers If inside an offer, will cancel inside the offer, otherwise it will cancel from main screen |
| `cancelOffer(int slot)` | Aborts the current offer in the slot specified in the Grand Exchange Works with buy/sell open or from main ge screen |
| `close()` | Closes the GrandExchange screen If close interface with ESC is enabled, will use esc key to close interface |
| `collect()` | Presses the collect button on the main interface, collects to inventory |
| `collectToBank()` | Uses the collect button to collect everything directly to the bank |
| `confirm()` | Confirms the offer in the Grand Exchange |
| `contains(int[] ids)` | see contains(Integer...) |
| `contains(Integer... ids)` | Checks if your collection contains one item which meets one of the specified ids. |
| `contains(String... names)` | Checks if your collection contains one item which meets one of the specified names. |
| `contains(Filter <Item> filter)` | Checks if your collection contains one item which meets the specified filter. |
| `getCurrentAmount()` | Deprecated. see getCurrentQuantity() |
| `getCurrentChosenItem()` | Gets the Item based on the ID returned from getCurrentChosenItemId() |
| `getCurrentChosenItemId()` | Gets the current chosen Item ID from the Set Up Buy/Sell Offer menu in the GE |
| `getCurrentChosenItemID()` | Deprecated. |
| `getCurrentPrice()` | Gets the currently set price of the offer based on the player settings |
| `getCurrentQuantity()` | Gets the current quantity set based on the player setting for current amount |
| `getDecreasePriceFivePercentButton()` | Gets the -5% price WidgetChild in the offer screen |
| `getDecreasePriceXPercentButton()` | Gets the -X% price WidgetChild in the offer screen |
| `getEnterAllButton()` | Gets the Enter All button in the offer screen |
| `getEnterPriceButton()` | Gets the Enter Price WidgetChild in the offer screen |
| `getEnterQuantityButton()` | Gets the WidgetChild of the "Enter Quantity" button |
| `getFirstOpenSlot()` | Gets the number of the first open slot |
| `getGuidePriceButton()` | Gets the Guide Price WidgetChild in the offer screen |
| `getIncreasePriceFivePercentButton()` | Gets the +5% price WidgetChild in the offer screen |
| `getIncreasePriceXPercentButton()` | Gets the +X% price WidgetChild in the offer screen |
| `getItem(int[] ids)` | See getItem(Integer...) |
| `getItem(Integer... ids)` | Gets a GrandExchangeItem that matches one of the given ids |
| `getItem(String... names)` | Gets a GrandExchangeItem that matches one of the given names |
| `getItem(Filter <Item> filter)` | Gets a GrandExchangeItem that matches one of the given filter |
| `getItemChildInSearch(String itemName)` | Gets the widget child of the item you searched for |
| `getItemChildInSearch(String name, int id)` | Gets the WidgetChild of the item you searched for |
| `getItems()` | Gets an array of the GrandExchangeItems |
| `getLastChosenItem()` | Gets the last chosen buy Item based on the ID returned from getLastChosenItemId() |
| `getLastChosenItemId()` | Gets the Item ID of the last chosen buy item |
| `getLastChosenItemID()` | Deprecated. |
| `getOfferFirstItemWidget()` | Gets the WidgetChild of the first item slot of the open offer |
| `getOfferSecondItemWidget()` | Gets the WidgetChild of the second item slot of the open offer |
| `getOfferText()` | Returns the text in the offer screen for whether it says Buy offer or Sell offer |
| `getOpenSlot()` | Returns the index of the open slot / offer |
| `getOpenSlots()` | Gets the number of slots that do not have an item and are enabled |
| `getSearchedItem()` | Gets the currently typed item in the search bar of the GE If no item is typed, widget is not visible, or buy screen is not open it will return "" NOT NULL |
| `getUsedSlots()` | Gets the number of slots that contain an item See slotContainsItem(int) |
| `goBack()` | Clicks the back button in the offer screen |
| `isBuyOpen()` | Checks if the buying screen of the Grand Exchange is open. |
| `isGeneralOpen()` | Checks if the general Grand Exchange is open |
| `isOpen()` | Returns if the Grand Exchange is open or not |
| `isReadyToCollect()` | Checks if any slot is done selling/buying and is ready to collect |
| `isReadyToCollect(int slot)` | Checks if the slot is done selling/buying and is ready to collect. |
| `isSearchOpen()` | Checks to see if the search item name widget is visible |
| `isSellOpen()` | Checks if the selling screen of the Grand Exchange is open |
| `isSlotEnabled(int slot)` | Checks if the specified slot is enabled. |
| `itemVisible(WidgetChild item)` | Checks to see if the searched item widget is visible in the item container under the search item text |
| `open()` | Opens nearest Grand Exchange booth |
| `open(String action)` | Opens the nearest Grand Exchange booth using the specified action. |
| `openBuyScreen(int slot)` | Opens the buy screen in the Grand Exchange of the specified slot. |
| `openSellScreen(int slot)` | Opens the sell screen in the Grand Exchange of the specified slot. |
| `openSlotInterface(int slot)` | Opens the general slot interface when a slot has an item in it |
| `readyToEnterAmount()` | Checks to see if the Dialogue can enter input with text "How many do you wish to" see Dialogues.canEnterInput(String) "How many do you wish to" |
| `readyToEnterPrice()` | Checks to see if the Dialogue can enter input with text "Set a price for each item:" see Dialogues.canEnterInput(String) "Set a price for each item:" |
| `scrollToItem(WidgetChild item)` | Scrolls to the provided widget in the item search container |
| `searchItem(String itemName)` | Searches for an item in the buy screen |
| `sellItem(int itemId, int amount, int price)` | Puts an item up for sale in the Grand Exchange |
| `sellItem(String itemName, int amount, int price)` | Puts an item up for sale in the Grand Exchange |
| `setPrice(int amount)` | Sets the price of the current buy/sell offer. |
| `setQuantity(int amount)` | Sets the quantity of a buy/sell offer |
| `slotContainsItem(int slot)` | Checks if there's an item in the slot specified |

---

### GrandExchangeItem

**Package:** `org.dreambot.api.methods.grandexchange`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.grandexchange.GrandExchangeItem → org.dreambot.api.methods.grandexchange.GrandExchangeItem  

**Methods:**
```java
int getAmount();
byte getByte();
int getId();
Item getItem();
String getName();
int getPrice();
int getSlot();
Status getStatus();
int getTransferredAmount();
int getTransferredValue();
int getValue();
boolean isBuyOffer();
boolean isCanceled();
boolean isReadyToCollect();
boolean isSellOffer();
```

| Method | Description |
|--------|-------------|
| `getAmount()` | Gets the Amount of the trading post item |
| `getByte()` | Gets the byte value of the state for the trading post item |
| `getId()` | Gets the ID of the trading post item |
| `getItem()` | Gets the Item that is in the trading post item slot |
| `getName()` | Gets the name of the trading post item |
| `getPrice()` | Gets the price of the trading post item |
| `getStatus()` | Gets the Status of the trading post item based on the current byte value. |
| `getValue()` | Gets the total value of the trading post item |
| `isBuyOffer()` | Checks if the offer is a buy offer |
| `isCanceled()` | Returns if the item was manually aborted |
| `isReadyToCollect()` | Returns whether the item is ready to be collected or not |
| `isSellOffer()` | Checks if the offer is a sell offer |

**Inherited methods:**
- From `Identifiable`: `getID`

---

### LivePrices

**Package:** `org.dreambot.api.methods.grandexchange`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.grandexchange.LivePrices → org.dreambot.api.methods.grandexchange.LivePrices  

**Methods:**
```java
static int get(int id);
static int get(String itemName);
static int get(Item item);
static int getHigh(int id);
static int getHigh(String itemName);
static int getHigh(Item item);
static int getLow(int id);
static int getLow(String itemName);
static int getLow(Item item);
```

| Method | Description |
|--------|-------------|
| `get(int id)` | This gets the average current price of an item by id |
| `get(String itemName)` | This gets the average current price of an item by name Note: You must have logged into the game as this method uses the cache to find item names |
| `get(Item item)` | This gets the average current price of an item |
| `getHigh(int id)` | This gets the current high end price of an item by id |
| `getHigh(String itemName)` | This gets the current high end price of an item by name Note: You must have logged into the game as this method uses the cache to find item names |
| `getHigh(Item item)` | This gets the current high end price of an item |
| `getLow(int id)` | This gets the current low end price of an item by id |
| `getLow(String itemName)` | This gets the current low end price of an item by name Note: You must have logged into the game as this method uses the cache to find item names |
| `getLow(Item item)` | This gets the current low end price of an item |

---

### Status

**Package:** `org.dreambot.api.methods.grandexchange`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<Status>org.dreambot.api.methods.grandexchange.Status → java.lang.Enum<Status> → org.dreambot.api.methods.grandexchange.Status → org.dreambot.api.methods.grandexchange.Status  

**Constants:**
```java
BUY
BUY_COLLECT
EMPTY
SELL
SELL_COLLECT
```

**Methods:**
```java
int getStatusValue();
static Status valueOf(String name);
static Status [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

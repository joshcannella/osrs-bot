# DreamBot API — input-keyboard

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### KeyboardProfile

**Package:** `org.dreambot.api.input.keyboard`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.input.keyboard.KeyboardProfile → org.dreambot.api.input.keyboard.KeyboardProfile  

**Methods:**
```java
static double getWordsPerMinute();
static boolean isMakeMistakes();
static void resetToDefaults();
static void setMakeMistakes(boolean mistakes);
static void setWordsPerMinute(double wpm);
```

| Method | Description |
|--------|-------------|
| `setWordsPerMinute(double wpm)` | Sets the word per minute and rebuilds the profile based on the new typing speed. |

---

### KeyboardTypingAlgorithm

**Package:** `org.dreambot.api.input.keyboard`  
**Type:** interface  

**Methods:**
```java
int handleTyping(String message);
void pressEnter();
void typeKey(Key key);
```

| Method | Description |
|--------|-------------|
| `handleTyping(String message)` | Handles typing of characters Intended use is to type one character at a type, but not a requirement Message will be truncated as per the number of characters typed before being re-called Enter is called separately if message is completed successfully |
| `pressEnter()` | Handles pressing enter, called when the message has been typed successfully if required |
| `typeKey(Key key)` | Handles typing of a singular Key If you require typing a character that is not in the Key list, the key events are able to send the raw character NOTE - the internal information of the event will not be correct if we do not have the key information for it. |

---

### StandardKeyboardAlgorithm

**Package:** `org.dreambot.api.input.keyboard`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.input.keyboard.StandardKeyboardAlgorithm → org.dreambot.api.input.keyboard.StandardKeyboardAlgorithm  

**Methods:**
```java
String getMistake(char c);
String getRandomCharInString(String string);
int handleTyping(String message);
void pressEnter();
void typeKey(char character);
void typeKey(Key key);
```

| Method | Description |
|--------|-------------|
| `handleTyping(String message)` | Handles typing of characters Intended use is to type one character at a type, but not a requirement Message will be truncated as per the number of characters typed before being re-called Enter is called separately if message is completed successfully |
| `pressEnter()` | Handles pressing enter, called when the message has been typed successfully if required |
| `typeKey(Key key)` | Handles typing of a singular Key If you require typing a character that is not in the Key list, the key events are able to send the raw character NOTE - the internal information of the event will not be correct if we do not have the key information for it. |

---

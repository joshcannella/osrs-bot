# DreamBot API — script-frameworks

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Branch

**Package:** `org.dreambot.api.script.frameworks.treebranch`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.script.frameworks.treebranch.Leaforg.dreambot.api.script.frameworks.treebranch.Branch → org.dreambot.api.script.frameworks.treebranch.Leaf → org.dreambot.api.script.frameworks.treebranch.Branch → org.dreambot.api.script.frameworks.treebranch.Branch  

**Methods:**
```java
Branch addLeaves(Leaf ... leaves);
void clear();
List<Leaf> getChildren();
int onLoop();
```

| Method | Description |
|--------|-------------|
| `addLeaves(Leaf ... leaves)` | Add leaves to this branch |
| `clear()` | Clears the leaves of this branch |
| `onLoop()` | Runs the first valid leaf that is found Sets the currently executing branch and leaf information with TreeScript.setCurrentBranchName(String) and TreeScript.setCurrentLeafName(String) |

**Inherited methods:**
- From `Leaf`: `getParent`, `getTree`, `isValid`, `setParent`

---

### Leaf

**Package:** `org.dreambot.api.script.frameworks.treebranch`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.script.frameworks.treebranch.Leaf → org.dreambot.api.script.frameworks.treebranch.Leaf  

**Methods:**
```java
Branch getParent();
TreeScript getTree();
abstract boolean isValid();
abstract int onLoop();
void setParent(Branch parent);
```

| Method | Description |
|--------|-------------|
| `getTree()` | Gets the TreeScript parent of this Leaf |
| `isValid()` | Boolean to check if this leaf should be run or not Implementation required |
| `onLoop()` | Main execution method for the leaf Implementation required |

---

### Loggable

**Package:** `org.dreambot.api.script.frameworks.utility`  
**Type:** interface  

**Methods:**
```java
default void debug(Object msg);
default void debug(String log, Object... objects);
default void error(Object msg);
default void error(String log, Object... objects);
default void error(String log, java.lang.Throwable exception, Object... objects);
default void info(Object msg);
default void info(String log, Object... objects);
default void log(Color color, String log, Object... objects);
default void log(Object object);
default void log(String log, Object... objects);
default void print(Object msg);
default void warn(String log, Object... objects);
```

| Method | Description |
|--------|-------------|
| `debug(Object msg)` | Deprecated. see debug(String, Object...) |
| `debug(String log, Object... objects)` | Formats the log string for incoming objects and logs it to Logger.debug(Object) . |
| `error(Object msg)` | Deprecated. see error(String, Object...) |
| `error(String log, Object... objects)` | Formats the log string for incoming objects and logs it to Logger.error(Object) . |
| `error(String log, java.lang.Throwable exception, Object... objects)` | Logs an error message along with an exception. |
| `info(Object msg)` | Deprecated. see info(String, Object...) |
| `info(String log, Object... objects)` | Formats the log string for incoming objects and logs it to Logger.info(Object) . |
| `log(Color color, String log, Object... objects)` | Formats the log string for incoming objects and logs it to Logger.log(Color, Object) . |
| `log(Object object)` | Deprecated. see log(String, Object...) |
| `log(String log, Object... objects)` | Formats the log string for incoming objects and logs it to Logger.log(Object) . |
| `print(Object msg)` | Deprecated. see log(String, Object...) |
| `warn(String log, Object... objects)` | Formats the log string for incoming objects and logs it to Logger.warn(Object) . |

---

### Root

**Package:** `org.dreambot.api.script.frameworks.treebranch`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.script.frameworks.treebranch.Leaforg.dreambot.api.script.frameworks.treebranch.Branchorg.dreambot.api.script.frameworks.treebranch.Root → org.dreambot.api.script.frameworks.treebranch.Leaf → org.dreambot.api.script.frameworks.treebranch.Branchorg.dreambot.api.script.frameworks.treebranch.Root → org.dreambot.api.script.frameworks.treebranch.Branch → org.dreambot.api.script.frameworks.treebranch.Root → org.dreambot.api.script.frameworks.treebranch.Root  

**Methods:**
```java
Root addBranches(Branch ... branches);
TreeScript getTree();
boolean isValid();
```

| Method | Description |
|--------|-------------|
| `addBranches(Branch ... branches)` | Adds Branch s to this Root Automatically sets the Branch parent to this root |
| `getTree()` | Gets the TreeScript of the Root |
| `isValid()` | Defaults isValid to true, no implementation required. |

**Inherited methods:**
- From `Branch`: `addLeaves`, `clear`, `getChildren`, `onLoop`
- From `Leaf`: `getParent`, `setParent`

---

### Sleepable

**Package:** `org.dreambot.api.script.frameworks.utility`  
**Type:** interface  

**Methods:**
```java
default void sleep(long ms);
default void sleep(long min, long max);
default boolean sleepUntil(Condition terminate, long timeout, long polling);
default boolean sleepUntil(Condition terminate, Condition reset, long timeout, long polling);
default boolean sleepWhile(Condition condition, long timeout, long polling);
default boolean sleepWhile(Condition condition, Condition reset, long timeout, long polling);
```

---

### TreeScript

**Package:** `org.dreambot.api.script.frameworks.treebranch`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.MethodProviderorg.dreambot.api.script.AbstractScriptorg.dreambot.api.script.frameworks.treebranch.TreeScript → org.dreambot.api.methods.MethodProvider → org.dreambot.api.script.AbstractScriptorg.dreambot.api.script.frameworks.treebranch.TreeScript → org.dreambot.api.script.AbstractScript → org.dreambot.api.script.frameworks.treebranch.TreeScript → org.dreambot.api.script.frameworks.treebranch.TreeScript  

**Methods:**
```java
Root addBranches(Leaf ... leaves);
void clear();
String getCurrentBranchName();
String getCurrentLeafName();
Root getRoot();
int onLoop();
void setCurrentBranchName(String currentBranchName);
void setCurrentLeafName(String currentLeafName);
```

| Method | Description |
|--------|-------------|
| `addBranches(Leaf ... leaves)` | Add Branch s or Leaf nodes to the root Returns the root node to allow chained adds if desired. |
| `clear()` | Clears the root's children See Branch.clear() |
| `onLoop()` | Primary execution loop of the script See Branch.onLoop() |

**Inherited methods:**
- From `AbstractScript`: `buildRandomManager`, `getCurrentState`, `getManifest`, `getRandomManager`, `getRandomThread`, `getScriptId`, `getScriptManager`, `getScriptTimer`, `getSDNName`, `getSDNParameters`, `getStoreId`, `getThreadURL`, `getVersion`, `isPaused`, `isUserSponsor`
  ...and 14 more
- From `MethodProvider`: `debug`, `error`, `info`, `log`, `print`, `sleep`, `sleep`, `sleepUntil`, `sleepUntil`, `sleepWhile`, `sleepWhile`
- From `Loggable`: `debug`, `debug`, `error`, `error`, `error`, `info`, `info`, `log`, `log`, `log`, `print`, `warn`
- From `Sleepable`: `sleep`, `sleep`, `sleepUntil`, `sleepUntil`, `sleepWhile`, `sleepWhile`

---

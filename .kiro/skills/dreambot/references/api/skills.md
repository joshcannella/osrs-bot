# DreamBot API — skills

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Skill

**Package:** `org.dreambot.api.methods.skills`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<Skill>org.dreambot.api.methods.skills.Skill → java.lang.Enum<Skill> → org.dreambot.api.methods.skills.Skill → org.dreambot.api.methods.skills.Skill  

**Constants:**
```java
AGILITY
ATTACK
CONSTRUCTION
COOKING
CRAFTING
DEFENCE
FARMING
FIREMAKING
FISHING
FLETCHING
HERBLORE
HITPOINTS
HUNTER
MAGIC
MINING
PRAYER
RANGED
RUNECRAFTING
SAILING
SLAYER
SMITHING
STRENGTH
THIEVING
WOODCUTTING
```

**Methods:**
```java
static Skill forId(int id);
int getBoostedLevel();
int getChildWidget();
int getExperience();
int getExperienceToLevel();
int getId();
int getLevel();
String getName();
String toString();
static Skill valueOf(String name);
static Skill [] values();
```

| Method | Description |
|--------|-------------|
| `forId(int id)` | Gets the skill that corresponds to the unique id. |
| `getChildWidget()` | Gets the skill's child widget id. |
| `getId()` | Gets the skill's unique id. |
| `getName()` | Gets the skill's name. |
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### SkillTracker

**Package:** `org.dreambot.api.methods.skills`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.skills.SkillTracker → org.dreambot.api.methods.skills.SkillTracker  

**Methods:**
```java
static long getGainedExperience(Skill s);
static int getGainedExperiencePerHour(Skill s);
static int getGainedLevels(Skill s);
static int getStartExperience(Skill s);
static int getStartLevel(Skill s);
static long getStartTime(Skill s);
static long getTimeToLevel(Skill s);
static boolean hasStarted(Skill skill);
static void reset(Skill s);
static void resetAll();
static void start();
static void start(boolean reset);
static void start(Skill s);
static void start(Skill ... skill);
static void start(Skill s, boolean reset);
```

| Method | Description |
|--------|-------------|
| `getGainedExperience(Skill s)` | Gets the gained experience since skill tracking began |
| `getGainedExperiencePerHour(Skill s)` | Gets gained experience per hour based on Skill start time |
| `getGainedLevels(Skill s)` | Gets gained levels of Skill |
| `getStartExperience(Skill s)` | Gets the starting experience value of the Skill. |
| `getStartLevel(Skill s)` | Gets the Starting level of the Skill |
| `getStartTime(Skill s)` | Gets the start time of the Skill |
| `getTimeToLevel(Skill s)` | Get the estimated amount of time in milliseconds until the next level at the current hourly rate. |
| `hasStarted(Skill skill)` | Checks if a skill has tracking started successfully |
| `reset(Skill s)` | Resets the current values of a Skill in the tracker |
| `resetAll()` | Sets all skill trackers to 0 |
| `start()` | Starts tracking all skills, will reset the tracker if already started |
| `start(boolean reset)` | Starts tracking all skills with option to reset values if skill has been previously started |
| `start(Skill s)` | Starts tracking a skill, will reset the tracker if already started |
| `start(Skill ... skill)` | Starts tracking one or more skill, will reset the tracker if already started |
| `start(Skill s, boolean reset)` | Starts tracking a skill with option to reset values if skill has been previously started |

---

### Skills

**Package:** `org.dreambot.api.methods.skills`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.skills.Skills → org.dreambot.api.methods.skills.Skills  

**Fields:**
```java
static int SKILLS_WIDGET
```

**Methods:**
```java
static int getBoostedLevel(Skill skill);
static int[] getBoostedLevels();
static int[] getExperience();
static int getExperience(Skill skill);
static int getExperienceForLevel(int level);
static int getExperienceToLevel(Skill skill);
static int getLevelForExperience(int experience);
static int[] getLevels();
static int getRealLevel(Skill skill);
static int getTotalLevel();
static boolean hoverSkill(Skill skill);
static boolean open();
```

| Method | Description |
|--------|-------------|
| `getBoostedLevel(Skill skill)` | Gets top level for skill |
| `getBoostedLevels()` | Get all boosted levels When level shows 11/9, this method returns 11 |
| `getExperience()` | Gets all experience |
| `getExperience(Skill skill)` | Gets experience for skill |
| `getExperienceForLevel(int level)` | Gets experience for level |
| `getExperienceToLevel(Skill skill)` | Gets experience to next level for skill |
| `getLevels()` | Gets all levels based on experience When skill shows 11/9, this method has 9 |
| `getRealLevel(Skill skill)` | Gets current level for skill |
| `getTotalLevel()` | Gets the total level of your character |
| `hoverSkill(Skill skill)` | hovers over skill |
| `open()` | Opens the skills Tab |

---

# DreamBot API — tabs

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### Tab

**Package:** `org.dreambot.api.methods.tabs`  
**Type:** enum  
**Hierarchy:** java.lang.Object → java.lang.Enum<Tab>org.dreambot.api.methods.tabs.Tab → java.lang.Enum<Tab> → org.dreambot.api.methods.tabs.Tab → org.dreambot.api.methods.tabs.Tab  

**Constants:**
```java
ACCOUNT_MANAGEMENT
CLAN
COMBAT
EMOTES
EQUIPMENT
FRIENDS
INVENTORY
LOGOUT
MAGIC
MUSIC
OPTIONS
PRAYER
QUEST
SKILLS
```

**Methods:**
```java
Tab get(int id);
int getId();
int getIndex();
int getKeyEvent();
int getParentId();
WidgetChild getWidgetChild();
boolean isDisabled();
boolean isOpen();
boolean open();
String toString();
static Tab valueOf(String name);
static Tab [] values();
```

| Method | Description |
|--------|-------------|
| `valueOf(String name)` | Returns the enum constant of this type with the specified name. |
| `values()` | Returns an array containing the constants of this enum type, in the order they are declared. |

---

### Tabs

**Package:** `org.dreambot.api.methods.tabs`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.tabs.Tabs → org.dreambot.api.methods.tabs.Tabs  

**Methods:**
```java
static Tab getOpen();
static boolean isDisabled(Tab tab);
static boolean isOpen(Tab tab);
static void logout();
static boolean open(Tab tab);
static boolean openWithFKey(Tab tab);
static boolean openWithMouse(Tab tab);
static void setShouldUseFKey(java.util.function.Predicate<Tab> shouldUseFKey);
```

| Method | Description |
|--------|-------------|
| `getOpen()` | Gets the currently open tab |
| `isDisabled(Tab tab)` | Checks if the tab is disabled. |
| `isOpen(Tab tab)` | Checks if tab is open |
| `logout()` | Opens logout tab and clicks logout button |
| `open(Tab tab)` | Opens specified tab |
| `openWithFKey(Tab tab)` | Opens the tab using F keys. |
| `openWithMouse(Tab tab)` | Opens the tab using the mouse. |
| `setShouldUseFKey(java.util.function.Predicate<Tab> shouldUseFKey)` | Sets the strategy for determining whether to use keyboard F-keys or mouse clicks to open tabs |

---

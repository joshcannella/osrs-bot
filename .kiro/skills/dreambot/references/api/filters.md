# DreamBot API — filters

<!-- Auto-generated by scripts/refresh-javadocs.py on 2026-04-06 03:05 UTC -->
<!-- Source: https://dreambot.org/javadocs -->

### ActionFilter

**Package:** `org.dreambot.api.methods.filter.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.filter.impl.ActionFilter<E> → org.dreambot.api.methods.filter.impl.ActionFilter<E>  

**Methods:**
```java
boolean match(E acceptable);
```

| Method | Description |
|--------|-------------|
| `match(E acceptable)` | If object match's arguments. |

**Inherited methods:**
- From `Filter`: `and`, `negate`, `or`

---

### AreaFilter

**Package:** `org.dreambot.api.methods.filter.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.filter.impl.AreaFilter<E> → org.dreambot.api.methods.filter.impl.AreaFilter<E>  

**Methods:**
```java
boolean match(E acceptable);
```

| Method | Description |
|--------|-------------|
| `match(E acceptable)` | If object match's arguments. |

**Inherited methods:**
- From `Filter`: `and`, `negate`, `or`

---

### EntityFilter

**Package:** `org.dreambot.api.methods.filter.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.filter.impl.EntityFilter<E> → org.dreambot.api.methods.filter.impl.EntityFilter<E>  

**Methods:**
```java
boolean match(E acceptable);
```

| Method | Description |
|--------|-------------|
| `match(E acceptable)` | If object match's arguments. |

**Inherited methods:**
- From `Filter`: `and`, `negate`, `or`

---

### Filter

**Package:** `org.dreambot.api.methods.filter`  
**Type:** interface  

**Methods:**
```java
default Filter <T> and(Filter <? super T> other);
boolean match(T acceptable);
default Filter <T> negate();
default Filter <T> or(Filter <? super T> other);
```

| Method | Description |
|--------|-------------|
| `match(T acceptable)` | If object match's arguments. |

---

### IdFilter

**Package:** `org.dreambot.api.methods.filter.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.filter.impl.IdFilter<E> → org.dreambot.api.methods.filter.impl.IdFilter<E>  

**Methods:**
```java
boolean match(E acceptable);
```

| Method | Description |
|--------|-------------|
| `match(E acceptable)` | If object match's arguments. |

**Inherited methods:**
- From `Filter`: `and`, `negate`, `or`

---

### ItemFilter

**Package:** `org.dreambot.api.methods.filter.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.filter.impl.ItemFilter<E> → org.dreambot.api.methods.filter.impl.ItemFilter<E>  

**Methods:**
```java
boolean match(E acceptable);
```

| Method | Description |
|--------|-------------|
| `match(E acceptable)` | If object match's arguments. |

**Inherited methods:**
- From `Filter`: `and`, `negate`, `or`

---

### NameFilter

**Package:** `org.dreambot.api.methods.filter.impl`  
**Type:** class  
**Hierarchy:** java.lang.Object → org.dreambot.api.methods.filter.impl.NameFilter<E> → org.dreambot.api.methods.filter.impl.NameFilter<E>  

**Methods:**
```java
boolean match(E acceptable);
```

| Method | Description |
|--------|-------------|
| `match(E acceptable)` | If object match's arguments. |

**Inherited methods:**
- From `Filter`: `and`, `negate`, `or`

---
